# ITR-3 Schedule-by-Schedule Implementation Tracker

**This is the single source of truth for the ITR-3 schedule-wise implementation push.** Updated after every schedule is completed end-to-end. Supersedes `Docs/ITR3_V2_PIPELINE_PRODUCTION_PLAN.md` and `Docs/ITR2_ITR3_V2_PIPELINE_PRODUCTION_PLAN.md`'s Phase 8 for status purposes (their PDF↔schema reconciliation table, reproduced below, remains valid reference material and is not being redone).

## Ground rules for this push

1. **One schedule at a time, in official PDF print order** (the table below). No skipping ahead.
2. For each schedule: read the official ITR-3 form PDF section + the official JSON schema block, then implement **every mandatory and optional field** — schema → draft mapper → calculator → ITD JSON builder. A schedule is not "done" until data flows for real from a `ReturnDraft` through to schema-valid JSON, not just until the builder can serialize a hand-constructed object.
3. **Validators are explicitly out of scope for this entire push.** `app/engine/validators/itr3/` is not touched until every schedule below is done and a separate validator pass begins. Do not add, remove, or edit any ITR-3 validator rule while working through this tracker.
4. **ITR-3 only.** If a defect is found in already-shipped ITR-1/ITR-2/ITR-4 code along the way, it is flagged in the "Cross-form issues found, not fixed" log at the bottom of this document and the user is asked explicitly before anything in those forms' code is touched. Never edit ITR-1/ITR-2/ITR-4 code as a side effect of ITR-3 work.
5. Frontend work in this push is limited to what's needed for real end-to-end data flow (wiring a field to the draft, fixing a mapping bug) — **not** visual/UX redesign, which is tracked separately (`Docs/FRONTEND_UI_UX_REDESIGN_AUDIT_AY2026_27.md`) and deliberately deferred.
6. After each schedule: run the full regression suite, confirm no new failures beyond the established baseline, update this tracker's status table and add a dated "Fix status" note under that schedule, then commit.

## Status legend

| Symbol | Meaning |
|---|---|
| ⬜ | Not started this push |
| 🔍 | In progress — being verified/implemented now |
| ✅ | Done — every mandatory + optional field verified end-to-end (mapper → calculator → builder → schema-valid JSON), confirmed against the official PDF and schema directly |
| ⛔ | Blocked — needs a decision (e.g. a cross-form fix, an architectural piece like a depreciation engine) before it can be closed |

## Schedule order and status

Reusing the verified, PDF-line-cited, schema-cross-referenced table from `Docs/ITR3_V2_PIPELINE_PRODUCTION_PLAN.md` §0A (confirmed accurate as reference by a dedicated re-audit — see `Docs/ITR3_CURRENT_STATUS_AY2026_27.md` §6). The "Prior finding" column carries forward what the 2026-09-17 status investigation (`Docs/ITR3_CURRENT_STATUS_AY2026_27.md`) already established — it is a starting hypothesis for this push to verify, not a conclusion to trust blindly.

| # | PDF section | Schema block(s) | REQ? | Status | Prior finding (to verify, not trust) |
|---:|---|---|:---:|:---:|---|
| 1 | Personal Information (A1–A18) | `PartA_GEN1` | REQ | ✅ | Closed 2026-09-17 — see fix log |
| 2 | Filing Status (A19) | `PartA_GEN1` + `PartA_GEN2` | REQ | ✅ | Closed 2026-09-17 — see fix log |
| 3 | Audit Information (A20) | `PartA_GEN2.AuditInfo` | REQ | ✅ | Closed 2026-09-17 — see fix log |
| 4 | Nature of Business or Profession | `PartA_GEN2.NatOfBus` | REQ | ✅ | Closed 2026-09-17 — see fix log |
| 5 | Part A-BS (Balance Sheet) | `PARTA_BS` | REQ | ✅ | Closed 2026-09-17 — see fix log |
| 6 | Part A-Manufacturing Account | `ManufacturingAccount` | opt | ✅ | Closed 2026-09-17 — see fix log |
| 7 | Part A-Trading Account | `TradingAccount` | opt | ✅ | Closed 2026-09-17 — see fix log |
| 8 | Part A-P&L | `PARTA_PL` | REQ | ✅ | Closed 2026-09-18 — see fix log |
| 9 | Part A-OI | `PARTA_OI` | opt (mandatory if 44AB-audited) | ✅ | Closed 2026-09-18 — see fix log |
| 10 | Part A-QD | `PARTA_QD` | opt (mandatory if 44AB-audited) | ✅ | Closed 2026-09-18 — see fix log |
| 11 | (Schedules banner) | — | — | N/A | Documentary only, no JSON |
| 12 | Schedule S — Salary | `ScheduleS` | opt | ✅ | Closed 2026-09-18 — see fix log |
| 13 | Schedule HP — House Property | `ScheduleHP` | opt | ✅ | Closed 2026-09-18 — see fix log |
| 14 | Schedule BP — Business/Profession | `ITR3ScheduleBP` | REQ | ✅ | Closed 2026-09-18 — see fix log (full closure of every item, including the previously-deferred cross-head reallocation, exempt income, Rule 7/7A/7B/8 composite income, and Part E intra-head loss set-off) |
| 15 | Schedule DPM | `ScheduleDPM` | opt | ✅ | Closed 2026-09-18 — see fix log (grouped with #16-18, one shared root cause) |
| 16 | Schedule DOA | `ScheduleDOA` | opt | ✅ | Closed 2026-09-18 — see fix log |
| 17 | Schedule DEP | `ScheduleDEP` | opt | ✅ | Closed 2026-09-18 — see fix log |
| 18 | Schedule DCG | `ScheduleDCG` | opt | ✅ | Closed 2026-09-18 — see fix log (also fixed a silent deemed-STCG understatement bug) |
| 19 | Schedule ESR | `ScheduleESR` | opt | ✅ | Closed 2026-09-18 — see fix log |
| 20 | Schedule CG | `ScheduleCGFor23` | opt | ✅ | Closed 2026-09-18 — see fix log. Disclosure items closed: A2/B2 (slump sale), A4/B3-proviso/B8 (NRI bare-entry figures), A9/B12 (DTAA-rate claims), A7/B10 (unutilized CGAS deposit), B4b (112A's own s.54F deduction), `TotDeductClaim`, dead-fallback-function `NameError` risk. **Severe tax-computation fix (2026-09-18, same day, user-authorized cross-form fix)**: section 112 (LTCG other than 112A, 12.5%) was never taxed at its own special rate at all — silently folded into slab-rate income instead. Fixed via a new shared `post_loss_cg_baskets()` (extracted from ITR-2's own calculator into `app/engine/schedules/capital_gains.py`) plus a full CYLA/BFLA/Schedule-SI rearchitecture mirroring ITR-2's own correct design, `is_resident` now wired into `compute_ltcg()`'s section 112(1)(a) second-proviso relief, and NRI-proviso-48/DTAA entries now actually taxed (previously disclosure-only). Current-year loss set-off matrix (`TotLossSetOff`) still deliberately deferred (same reasoning as Schedule BP's own Part E). **Comprehensive combined ITR-2+ITR-3 Schedule CG test suite added 2026-09-18** (`tests/test_schedule_cg_comprehensive.py`, 44 test functions/79 cases, user-requested) — surfaced three further genuine defects (formerly items 5-7 in "Cross-form issues found, not fixed" above). **All three fixed 2026-09-19** at explicit user request: ITR-3's BFLA `str(enum)` bug (brought-forward losses previously had zero effect), the shared `post_loss_cg_baskets()`'s CYLA-vs-BFLA staleness asymmetry (a pre-existing ITR-2 bug affecting both forms — user explicitly authorized touching ITR-2), and ITR-3's missing STCG-land-54B exemption wiring. Suite grew to 83 cases, all confirmed genuinely failing pre-fix via `git stash` |
| 21 | Schedule 112A | `Schedule112A` | opt | ⬜ | Real |
| 22 | Schedule 115AD | `Schedule115AD` | opt | ⬜ | Real (FII/FPI bucket, thin real-world use for ITR-3 individual/HUF filers) |
| 23 | Schedule VDA | `ScheduleVDA` | opt | ⬜ | Real |
| 24 | Schedule OS | `ScheduleOS` | opt | ⬜ | Not independently verified this push |
| 25 | Schedule CYLA | `ScheduleCYLA` | REQ | ⬜ | Real |
| 26 | Schedule BFLA | `ScheduleBFLA` | REQ | ⛔ | **Confirmed bug**: `BusProfExclSpecProf.IncBFLA` hardcoded to always-zero (`itr3.py:941`, both operands ×`Decimal("0")`). `bf_losses` never mapped from draft at all |
| 27 | Schedule CFL | `ScheduleCFL` | opt | ⛔ | Downstream of unmapped `bf_losses` — no ITR-3 return can currently carry forward/set off a prior-year loss |
| 28 | Schedule UD | `ITR3ScheduleUD` | opt | ⛔ | Broken at both layers: calculator hardcodes `unabsorbed_dep_setoff = 0` with a `# Simplified` comment; no draft mapper either |
| 29 | Schedule ICDS | `ScheduleICDS` | opt | ⬜ | Passthrough only |
| 30 | Schedule 10AA | `Schedule10AA` | opt | ⬜ | Deduction amount comes from generic Chapter VI-A breakdown, not this typed schedule |
| 31 | Schedule 80G | `Schedule80G` | opt | ⬜ | Frontend bug found: ARN field renders unconditionally, should be category-D-only |
| 32 | Schedule 80GGA | `Schedule80GGA` | opt | ⬜ | Not independently verified this push |
| 33 | Schedule 80GGC | `Schedule80GGC` | opt | ⬜ | Not independently verified this push |
| 34 | Schedule 80DD | `Schedule80DD` | opt | ⬜ | Frontend: disability-type dropdown collapses 10 named conditions to 2 |
| 35 | Schedule 80U | `Schedule80U` | opt | ⬜ | Not independently verified this push |
| 36 | Schedule RA | `Schedule80RA` | opt | ⬜ | Not independently verified this push |
| 37 | Schedule 80-IA | `Schedule80_IA` | opt | ⬜ | Deduction amount via generic breakdown, not this typed schedule |
| 38 | Schedule 80-IB | `Schedule80_IB` | opt | ⬜ | Same |
| 39 | Schedule 80-IE | `Schedule80_IC` | opt | ⬜ | Same; naming divergence (PDF "80-IE" = schema `Schedule80_IC`) already documented |
| 40 | Schedule VI-A | `ScheduleVIA` + sub-blocks | opt | ⬜ | Real; frontend: **Section 80CCH missing entirely** |
| 41 | Schedule AMT | `ScheduleAMT` | opt | ⬜ | Real |
| 42 | Schedule AMTC | `ScheduleAMTC` | opt | ⛔ | **Confirmed gap**: `amt_input`/prior-year AMT credit never mapped from draft — no ITR-3 return can claim brought-forward AMT credit |
| 43 | Schedule SPI | `ScheduleSPI` | opt | ⬜ | Real |
| 44 | Schedule SI | `ScheduleSI` | opt | ⬜ | Real |
| 45 | Schedule IF | `ScheduleIF` | opt | ⛔ | **Confirmed dead field**: draft's `partnerInFirmEntries` fully modeled, never read anywhere in `app/engine` |
| 46 | Schedule EI | `ScheduleEI` | opt | ⛔ | **Confirmed gap**: calculator + builder real; `agricultural_income`/`exempt_income` never mapped from draft. Frontend: agri-income threshold shown as ₹5,000 instead of ₹5,00,000 (100x) |
| 47 | Schedule PTI | `SchedulePTI` | opt | ⬜ | Partial — disclosure only, thin GTI integration |
| 48 | Schedule TPSA | `ScheduleTPSA` | opt | ⬜ | Schema/builder real; calculator never reads it (no tax computation at all) |
| 49 | Schedule FSI | `ScheduleFSI` | opt | ⬜ | Real (reused ITR-2 plumbing) |
| 50 | Schedule TR | `ScheduleTR1` | opt | ⬜ | Real |
| 51 | Schedule FA | `ScheduleFA` | opt | ⬜ | Real |
| 52 | Schedule 5A | `Schedule5A2014` | opt | ⬜ | Real |
| 53 | Schedule AL | `ScheduleAL` | opt | ⬜ | Real; ₹1cr threshold fix already confirmed correct (2026-09-14) |
| 54 | Schedule GST | `ScheduleGST` | opt | ⬜ | Passthrough disclosure only |
| 55 | ESOP | `ScheduleESOP` | opt | ⬜ | Real |
| 56 | Part B-TI | `PartB-TI` | REQ | ⬜ | Real |
| 57 | Part B-TTI | `PartB_TTI` | REQ | ⬜ | Real |
| 58 | TRP details | `TaxReturnPreparer` | opt | ⬜ | Not independently verified this push |
| 59 | Section 17 Tax Payments | `ScheduleIT`+`TDS1`+`TDS2`+`TDS3`+`TCS` | opt | ⬜ | Real (reused ITR-2 plumbing); frontend: `ITR3TaxPaymentEditor` tab is dead code (defined, never rendered) — needs fixing as part of this schedule's end-to-end close |
| 60 | Verification | `Verification` | REQ | ⬜ | Real |

**Schema-only blocks with no standalone PDF section** (`CreationInfo`, `Form_ITR3`, the 80C-family sub-blocks under Schedule VI-A) are closed alongside the phase that owns them (60 and 40 respectively) per §0B of the old plan doc — not tracked as separate rows here.

---

## Cross-form issues found, not fixed

Issues spotted in already-shipped ITR-1/ITR-2/ITR-4 code while working ITR-3, per the ground rule above — flagged here and to the user explicitly, never fixed as a side effect.

1. **ITR-2, live regression, not caused by this push**: `tests/test_draft_to_itr2_input.py::test_other_exempt_income_preserves_per_clause_classification` fails — `app/engine/draft_to_itr2_input.py:701` (`_map_exempt_income()`) reads `row.natureOfIncome`, but `ExemptIncomeEntry` (`app/schemas/return_draft.py:1200-1204`) has no such attribute — it has `category`/`subCategory`/`description`/`grossAmount` instead. This was already surfaced during the ITR-3 status investigation (`Docs/ITR3_CURRENT_STATUS_AY2026_27.md` §2) and confirmed present in the full regression run after this push's Schedule 1 fix (still exactly this one extra failure, nothing new). **Not fixed — awaiting explicit go-ahead to touch ITR-2 code.**

2. **ITR-2 (and now ITR-3, via reuse), found while closing Schedule CG's A7/B10**: ITR-2's own B10-equivalent item (unutilized LTCG CGAS deposit) is itself still hardcoded (`"UnutilizedLtcgFlag": "N", "AmtDeemedLtcg": 0, "TotalAmtDeemedLtcg": 0` in `itd/itr2.py`) despite the identical frontend `ltUnutilized` row data already being captured — ITR-3's own LTCG side was built fresh this round rather than ported, since there was nothing working to port. **Not fixed in ITR-2 — flagged, not touched.**

3. **ITR-2 (and now ITR-3, via reuse), found while closing Schedule CG's A9/B12**: the shared `CGDtaaEntry.item_no_incl` field (`app/schemas/itr2.py`) has no enum constraint at the Pydantic level, but the official JSON schema restricts this field to a fixed, form-specific list of item-number labels (e.g. ITR-3's own STCG table only accepts `A1e`/`A2c`/`A3ie`/`A3iie`/`A4a`/`A4b`/`A5e`/`A6g`/`A7`/`A8a`/`A8b`/`A8c`) — and the shared mapper's own default for a blank frontend field, `"NA"`, is not a member of that enum for either form. A real taxpayer relying on that default would produce schema-invalid JSON for this one field. Confirmed by direct schema validation while writing this round's own DTAA test (caught immediately: `'NA' is not one of [...]`). **Not fixed — the mapper lives in `draft_to_itr2_input.py`, shared code, awaiting explicit go-ahead to touch it.**

4. **ITR-3-only (no ITR-2 equivalent — ITR-2 always passes `_ZERO` for this field, since it has no business-income concept at all), found while rearchitecting Schedule CG's special-rate tax computation**: `calculators/itr3.py`'s CYLA wiring passes `non_spec_biz_loss=pgbp.part_e_loss_remaining` — a POSITIVE magnitude — into `CYLAInput.non_spec_biz_loss`, but `loss_setoff/cyla.py`'s own `_loss(value) = max(0, -value)` helper only extracts a nonzero magnitude from a NEGATIVE signed value (matching every sibling field's own convention, e.g. `hp_loss`). The practical effect: a non-speculative business loss currently NEVER gets set off against ANY other head (HP/CG/etc.) via CYLA for ITR-3 — `non_spec_biz_setoff` is silently always zero regardless of a real, nonzero business loss. Confirmed live via a direct calculator call (a ₹30L business loss alongside an ₹80L LTCG produced `cyla_total_set_off == 0`). This is a genuine, severe, ITR-3-specific defect, but it is a Schedule BP/CYLA cross-head wiring bug, not a Schedule CG tax-RATE bug — out of scope for this round's user-authorized fix (which was scoped explicitly to "the capital-gains tax rate bug"). **Not fixed — flagged for explicit user go-ahead before touching business-loss CYLA wiring.**

5. ~~ITR-3-only: `bf_list`'s `str(item.head)` never matches `bfla.py`'s dispatch~~ — **RESOLVED 2026-09-19**, user explicitly requested the fix. See Schedule 20's own dated fix-log update entry below for the full write-up.

6. ~~Pre-existing in ITR-2, inherited by the shared `post_loss_cg_baskets()`: STCG-side buckets read pre-BFLA values~~ — **RESOLVED 2026-09-19**, user explicitly requested the fix (an explicit, one-time exception to this section's own "flag, don't touch ITR-2" rule). See Schedule 20's own dated fix-log update entry below.

7. ~~ITR-3-only: section 54B on short-term land/building gain had zero tax effect~~ — **RESOLVED 2026-09-19**, user explicitly requested the fix. See Schedule 20's own dated fix-log update entry below.

8. **Shared by BOTH forms, found via a full re-audit of Schedule CG against the official form text (2026-09-19, user requested "recheck ... to be very very sure")**: section 50CA (deemed consideration for unquoted shares, "higher of consideration or FMV") is correctly implemented in `deemed_consideration_50ca()` (`capital_gains.py`) and correctly CALLED by the disclosure builder (`itd/cg_shared.py:219`) — but is **never called by the actual tax-computation path** (ITR-2's `_classify()` else-branch; ITR-3's `other_asset_gain()`) for either form's generic "other assets" bucket (A5/B8 ITR-2, A6/B9 ITR-3). Both paths use the raw `full_consideration` directly. Live-confirmed (verified independently, not just from the audit report): an unquoted-share sale with consideration ₹1,00,000, cost ₹50,000, section-50CA FMV ₹20,00,000 (correct taxable gain ₹19,50,000 per the higher-of rule) produces `capital_gains_income == 50000` on BOTH ITR-2 and ITR-3 — the disclosed JSON figure and the actual taxed amount silently diverge, and a taxpayer selling unquoted shares below FMV is undertaxed by the full gap with no cap. **Not fixed — flagged for explicit user go-ahead** (this is a shared-code, both-forms, live-tax-computation change).

9. **ITR-2-only, found via the same re-audit**: section 94(7)/94(8) disallowed-loss addback (form item A5d/e: "STCG on assets other than A1-A4 = 5c + 5d", where 5d is the disallowed loss entered positive and ADDED) is present on ITR-3's own `other_asset_gain()` but MISSING from ITR-2's own `_classify()` else-branch (`capital_gains.py`, generic "other assets" gain = `full_consideration - cost - expenditure`, no reference to `loss_disallowed_94_7_94_8` at all). Live-confirmed: jewellery STCG with consideration ₹5,00,000, cost ₹3,00,000, `loss_disallowed_94_7_94_8=₹50,000` (correct A5e = 5c+5d = ₹2,50,000 per the form's own formula) produces `capital_gains_income == 200000` on ITR-2 — the addback is silently dropped, understating tax. **Not fixed — flagged for explicit user go-ahead** (ITR-2's own already-shipped, live-audited code).

10. **ITR-3-only (a genuine gap in ITR-3's own form, no ITR-2 equivalent needed), found via the same re-audit**: ITR-3's land/building exemption items allow a WIDER section set than the shared `CGTransaction`/`ExemptionResult` scalar fields support — confirmed directly against the form text: STCG item A1d = "Deduction under section 54B/54G/54GA" (3 sections), LTCG item B1d = "Deduction under section 54/54B/54D/54EC/54F/54G/54GA" (7 sections) — but `CGTransaction` only has `deduction_us54`/`deduction_us54b`/`deduction_us54ec`/`deduction_us54f` (4 scalars), with no field or aggregation slot anywhere for 54D, 54G, or 54GA specifically for the land/building bucket (the "other assets" bucket's free-form `other_assets_exemption_section`/`_amount` pair already correctly supports these three sections, per its own `ITR3_OTHER_ASSETS_*_EXEMPTION_SECTIONS` constants — this gap is land/building-specific). ITR-2's own land/building items only ever allow 54/54B/54EC/54F (confirmed against its own form text), so ITR-2 has no practical gap here — the underlying schema limitation is shared code, but only ITR-3 taxpayers can actually hit it. Effect: a taxpayer claiming 54D/54G/54GA against land/building capital gains on ITR-3 gets zero exemption benefit, silently. **Not fixed — flagged for explicit user go-ahead** (needs new schema fields, not just a wiring fix).

11. **ITR-3-only, sharpened characterization of an already-tracked gap, found via the same re-audit**: slump sale (`cg_slump_sale_stcg`/`_ltcg`) and unutilized-CGAS deposits (`cg_stcg_unutilized_deposits`/`cg_ltcg_unutilized_deposits`) were already known to be disclosure-only (zero tax effect, tracked in Schedule 20's own status-row history). The re-audit additionally found a THIRD such item, `pti_entries` (`ITR3Input.pti_entries`, confirmed by direct grep to never be referenced anywhere in `calculators/itr3.py`), and a sharper consequence than "no tax effect": since `stcg.total_stcg`/`ltcg.total_ltcg` never include any of these three components, the filed JSON's own `TotalSTCG`/`TotalLTCG` fields will not equal the form's own stated total-STCG/total-LTCG sum formula whenever any of the three is nonzero — an internal arithmetic inconsistency in the return itself, not just an undertaxation risk. **Not fixed — flagged for explicit user go-ahead.**

---

## Schedule-by-schedule fix log

### Schedule 1 — Personal Information (A1–A18) — ✅ closed 2026-09-17

**What was found** (comparing `Docs/ITR3_FIELD_BY_FIELD_GUIDE_1_PERSONAL_INFO_TO_HP.md` §1 and the official schema's `PersonalInfo`/`AlternateAddress` definitions directly against `app/engine/itd/itr3.py::_parta_gen1`):
- A6a (building/village name) and A7a (road/street) were captured nowhere — the builder hardcoded `ResidenceName`/`RoadOrStreet` to `""` unconditionally.
- A16 (Aadhaar) was read by `_parta_gen1`'s signature but never actually sourced from `ITR3Input` in the canonical path — always absent from real filings.
- A17's office/residence phone (schema `Address.Phone`) had no code path at all; its secondary-mobile columns (`CountryCodeMobileNoSec`/`MobileNoSec`) were hardcoded to `0`/`0` unconditionally.
- A18's secondary email (`EmailAddressSec`) was never emitted.
- **A5b–A13b, the "Secondary Address"**: `SecondaryAdd` was hardcoded `"N"` regardless of the taxpayer's actual declaration, and — the most serious finding — whenever `secondary_add == "Y"` was forced via a non-canonical caller, `AlternateAddress` was populated as a **literal copy of the primary address**, not real secondary-address data. There was no source data path for a genuine alternate address anywhere in the pipeline.
- Root cause for all of the above: `app/schemas/itr3.py`'s `ITR3Input` had no fields at all for any of this (only the eight primary-address scalars), and `app/engine/draft_to_itr3_input.py` never read the corresponding `ReturnDraft.personal.*` fields — even though every one of those fields (`aadhaar`, `secondaryEmail`, `secondaryMobile`(+country code), `landlineStdCode`/`landlinePhoneNo`, `residenceName`, `roadOrStreet`, `secondaryAddressDifferent`, `alternateAddress`) already existed on the shared `PersonalInfo` draft model. **No frontend/draft-schema changes were needed** — this was purely a backend mapper + builder gap.

**What was fixed:**
- `app/schemas/itr3.py`: added `residence_name`, `road_or_street`, `zip_code`, `mobile_country_code`, `assessee_aadhaar`, `office_phone_std_code`, `office_phone_no`, `secondary_mobile_country_code`, `secondary_mobile_no`, `secondary_email`, `secondary_address_different`, and the nine `alternate_*` fields to `ITR3Input`.
- `app/engine/draft_to_itr3_input.py`: mapped all of the above from `draft.personal.*`/`draft.personal.alternateAddress.*` (soft/optional, matching the mapper's existing style for this section — hard requiredness stays the builder's job, as it already was for the original 8 fields).
- `app/engine/itd/itr3.py`: `_parta_gen1()`/`build_itr3_json()` rewritten to emit every field for real: `ResidenceName`/`RoadOrStreet` from sourced data; `AadhaarCardNo` from the typed input; `Address.Phone` emitted only when a real STD code + number exist; secondary mobile/email sourced for real; `SecondaryAdd` reflects the taxpayer's actual flag; `AlternateAddress` built from genuinely distinct secondary-address data, never copied from the primary address. Added a fail-closed check: if `secondary_address_different` is True but the required alternate-address fields weren't actually sourced, `build_itr3_json` now raises rather than silently omitting or fabricating the block. `AlternateAddress.PinCode`/`ZipCode` are only emitted when real (neither is schema-required — no fabricated PIN default was introduced for the new code, unlike the pre-existing primary-address fallback which was left as dead-path legacy scaffolding, see below).
- Deliberately **not** touched: the primary `Address`'s existing fabricated-default kwargs (`locality: str = "Locality"`, `pin_code` falling back to `110001`, etc.) on `build_itr3_json`'s legacy (`typed_input=None`) call path — every real canonical caller already passes `typed_input`, which hard-requires these via the pre-existing `required_identity` check before `_parta_gen1` is ever reached, so this fallback code is effectively dead in production; changing pin_code's required-ness policy (e.g. to support a foreign-only ZIP-code address) is a separate design decision, not evidenced as broken today, and was left alone to keep this fix scoped to real field-completeness, not a required-ness policy change.

**Verification**: two new tests in `tests/test_itr3_canonical_foundation.py` — `test_itr3_personal_info_optional_fields_reach_typed_input_and_json` (every new field set on a realistic draft, asserted present in both `typed_input` and the emitted JSON, validated against the official `PersonalInfo` schema sub-definition directly via `jsonschema.Draft4Validator` — scoped to this schedule since the full document doesn't validate yet yet, pending Schedules 5/8/9/25/26/24 etc.) and `test_itr3_secondary_address_flag_without_data_fails_closed` (confirms the new fail-closed guard). Both confirmed genuinely failing pre-fix via `git stash`. Full combined regression (`test_itr1/2/3/4_*.py` + filing-gateway/AMT/CYLA/BFLA/capital-gains suites): 1313 passed, same 10 pre-existing baseline failures plus the one already-flagged, pre-existing, unrelated ITR-2 regression (see "Cross-form issues" above) — no new failures.

**Correction found during Schedule 2 work (2026-09-17), applied retroactively to Schedule 1**: while reading ITR-2's own `_part_a_gen1`-equivalent builder as a reference, found a live-UAT-verified fact ITR-2's own code comment documents (2026-09-13, PAN GOYPT2026A): ITD's real Type-2 `validateItr` **rejects an entirely-absent `AlternateAddress` block**, regardless of the `SecondaryAdd` flag's value — even though neither is schema-required. Schedule 1's original fix omitted `AlternateAddress` whenever the taxpayer had no genuinely distinct secondary address, which — since ITR-3 shares the identical `PersonalInfo`/`AlternateAddress` schema shape with ITR-2 — would almost certainly hit the same live rejection. Revised to match ITR-2's proven fallback exactly: `SecondaryAdd` is now always `"Y"`, and `AlternateAddress` is always emitted, falling back to a copy of the primary address when no genuinely distinct one exists (the fail-closed guard for "declared different but didn't provide one" stays). Also found and fixed, while reading the schema's `Status` enum for A14: `assessee_status` (A1-A18's own Individual/HUF flag) was **never wired at all** — `build_itr3_json` never derived it from `typed_input`, so every canonical filing silently emitted `"I"` regardless of the real `draft.personal.assesseeStatus`. Added `ITR3Input.assessee_status`, mapped from the draft, and wired into the canonical builder path. Both corrections covered by `test_itr3_no_secondary_address_still_emits_alternate_address_as_primary_copy` (new) and the existing Schedule 1 tests, all confirmed passing; full detail in the Schedule 2 entry below.

### Schedule 2 — Filing Status (A19) — ✅ closed 2026-09-17

**What was found**: this is the largest single schedule so far (A19(a) through A19(q) — due date/filing section, the full Form 10-IEA regime-cascade, seventh-proviso-139(1) declarations, revised/defective/notice metadata, residential-status conditions/jurisdiction/stay-days, section 115H, Portuguese Civil Code, representative assessee, company directorships, partner-in-firm, unlisted-equity holdings, NRI permanent-establishment/significant-economic-presence, IFSC-unit flag, FII/FPI + SEBI registration, and LEI). Before this fix, `_parta_gen1`'s `FilingStatus` block was an 8-key stub — only the schema's bare `required` list, all but two hardcoded (`SeventhProvisio139: "N"`, `HeldUnlistedEqShrPrYrFlg: "N"`, `ForeignExchangeFlag: "N"`, `FiiFpiFlag: "N"`, a static due date) regardless of the real return. Every one of the ~14 optional sub-blocks above was completely absent from every real filing.

Cross-referenced the `ReturnDraft` schema first (`app/schemas/return_draft.py`'s `FilingStatus`/`PersonalInfo` classes) and confirmed — as with Schedule 1 — that the frontend already captures literally every one of these fields (this was clearly built out during ITR-2's own Phase 3 work, on the shared draft classes both forms use). **No frontend/draft-schema changes were needed for this schedule either** — purely a backend mapper + builder gap.

**What was fixed:**
- `app/schemas/itr3.py`: added ~45 new `ITR3Input` fields covering the full A19 sub-item list (Form 10-IEA cascade × 12, seventh proviso × 8, revised/notice metadata × 4, residential-status × 4, 115H, Portuguese code, representative × 4, director flag + entries, partner-in-firm flag + entries, unlisted-equity flag + entries, PE/SEP × 4, IFSC flag, FII/FPI + SEBI, LEI × 2).
- `app/engine/draft_to_itr3_input.py`: mapped every field from `draft.filing.*`/`draft.personal.*`. **Found and fixed a real mapper bug of my own mid-implementation**: ten of these fields (`conditionsResStatus`, `jurisdictionResidenceEntries`, `totalStayIndiaPrevYr`, `totalStayIndia4PrecYr`, `benefitUs115H`/`benefitUs115HAnswered`, `nriPEinIndia`, `nriSEPinIndia`, `aggrPaymentTransac`, `numberOfUsers`, `foreignExchangeFlag`, `isFiiFpi`, `sebiRegistrationNumber`, `leiNumber`, `leiValidUptoDate`, `portugueseCivilCodeApplies`) live on the draft's `FilingStatus` class, not `PersonalInfo` — an initial pass wrote `draft.personal.X` for all of them, which fails immediately (`AttributeError`, caught by the very first test run, before any commit).
- `app/engine/itd/itr3.py`: `_parta_gen1` now takes the full `typed_input: ITR3Input | None` object (matching the pattern every sibling builder function already uses) and builds the complete `FilingStatus` block from it. Ported two hard-won, live-UAT-verified patterns from ITR-2/ITR-4's own builders rather than reinventing them: (1) the mutually-exclusive Form 10-IEA "A23" branch discipline from ITR-4's builder (CBDT rules #353-364 — emitting both the "filed old regime earlier, now re-entering new" branch and the "never filed old regime, filing old/new this year" branch together was rejected live by ITD, 2026-09-04, PAN SRGPZ2026C — ITR-3 shares the identical field set); (2) 115H's "answered vs. never asked" Y/N semantics and the `SebiRegnNo` (not `SEBIRegNo`) field-name spelling, both from ITR-2's own already-fixed history.

**Verification**: three new tests — `test_itr3_filing_status_old_regime_currently_filing_reaches_json` and `test_itr3_filing_status_earlier_old_regime_then_reentered_new_reaches_json` (the two mutually-exclusive Form 10-IEA branches, each asserting the *other* branch's fields are completely absent, not just `"N"`), and `test_itr3_filing_status_seventh_proviso_representative_director_partner_reach_json` (every remaining A19 sub-item on one realistic draft). All three validated against the official `FilingStatus` schema sub-definition directly and confirmed genuinely failing pre-fix via `git stash`. ITR-3 suite: 62 passed (up from 56 at Schedule 1's close). Full combined regression: 1317 passed, same 10-failure baseline + the one already-flagged unrelated ITR-2 regression — no new failures.

### Schedule 3 — Audit Information (A20) — ✅ closed 2026-09-17

**What was found**: `AuditInfo` (both the `app/schemas/itr3.py` typed model and the `_parta_gen2()` builder) covered only 5 of roughly 25 real schema fields. Two of those five weren't even real: `AgrOFAllAmtsRcvd`/`AgrOFAllPayMade` (A20(a2ii)/(a2iii), the cash-receipt/payment percentage question that determines whether a mid-size taxpayer is 44AB-liable) were **hardcoded to `"Upto5Per"` unconditionally**, regardless of the actual return — a return where the real answer was "more than 5%" (the answer that actually *triggers* audit liability) would have silently disclosed the wrong, audit-avoiding answer. Entirely absent: the turnover band (a2i), the 44AB-liability reason (b(i)/(ii)/(iii) + the `BiiDetails` presumptive-section sub-flags), the entire audit-report detail block (c(1)-(4): furnish date, acknowledgement number, auditor name, auditor PAN/Aadhaar), the section-92E audit detail (d(ii): date + acknowledgement, distinct from merely being *liable* under 92E), and both "other audit report" tables (d(iii) — other-section audit reports for 10A/10AA/44DA/50B/80-IA-family/80JJAA/80LA/115JC; (e) — audit under an Act other than the Income-tax Act, e.g. Companies Act/GST/SEBI Act).

`app/schemas/return_draft.py`'s `ITR3AuditInfo` (plus its two row types, `OtherAuditReportEntry` and `AuditUnderOtherActEntry`) already captures every one of these fields in full — confirmed, as with Schedules 1-2, this was purely a backend mapper + builder gap, no frontend work needed.

**What was fixed:**
- `app/schemas/itr3.py`: extended `AuditInfo` from 5 to 24 fields, covering the full A20(a)-(e) list.
- `app/engine/draft_to_itr3_input.py`: `_audit_info()` rewritten to extract every field from both of this mapper's existing two data sources — the raw official-schema-shaped "workspace" object (`draft.itr3BusinessWorkspace.core["PartA_GEN2"]["AuditInfo"]`, when a UI editor has written directly in CBDT shape) and the canonical typed `draft.itr3AuditInfo` fallback — matching the dual-path pattern this function already had for the 5 original fields, just extended to the other 19.
- `app/engine/itd/itr3.py`: `_parta_gen2()` rewritten to emit every field for real, each one only when the taxpayer's actual data supports it (the audit-report detail block only when `audited_by_accountant` is true; the 92E detail only when both the date and acknowledgement number are genuinely present; both array tables built from real entries, empty rows filtered out) — never fabricated, matching this schedule's own "omit, don't guess" discipline. Also fixed the `AuditAccountantFlg` field, which was previously wired to `audit.account_audited` (the SAME boolean already used for the separate, differently-named `AccountAuditFlag` field) — now correctly reads the draft's own distinct `audited_by_accountant` fact.

**Verification**: two new tests — `test_itr3_audit_info_full_44ab_disclosure_reaches_json` (every field set on a realistic 44AB/92E-audited taxpayer, asserted through to the JSON, including both array tables) and `test_itr3_audit_info_not_audited_omits_report_detail_not_fabricates` (a non-audited taxpayer gets no fabricated audit-report detail — a confirmation test: this specific behavior was already correct before the fix too, since those fields were simply never emitted at all; it now locks in that the *fix* didn't accidentally start fabricating them once real data-sourcing was added elsewhere in the same function). Both validated against the official `AuditInfo` schema sub-definition; the first confirmed genuinely failing pre-fix via `git stash`. ITR-3 suite: 64 passed (up from 62). Full combined regression: 1319 passed, same 10-failure baseline + the one already-flagged unrelated ITR-2 regression — no new failures.

### Schedule 4 — Nature of Business or Profession — ✅ closed 2026-09-17

**What was found**: a small schedule (just `Code`/`TradeName1`/`Description` per row), but with two real bugs, not just a completeness gap.
1. **`TradeName1`** ("Trade name of the proprietorship, if any") was captured on the draft (`ITR3NatureOfBusiness.tradeName`) but had **no field on the typed `NatureOfBusiness` schema at all**, and both mapper code paths silently dropped it — confirmed directly: the pre-existing test `test_itr3_builder_maps_parta_gen2_workspace` had been setting `tradeName="Tech"` on its own test draft since before this push and never noticed it never reached the output, because the test never asserted on that field.
2. **Three legitimate CBDT business-nature codes were silently dropped from every return that used them.** The official schema enum includes three disambiguation codes with an `_N` suffix — `16019_1` ("Medical Profession", distinct from the base `16019`), `20023_1` ("Sports Management"), `21008_1` ("Event Management"). Both the mapper's row filter (`row.code.isdigit()` / `str(...).isdigit()`) and the typed `NatureOfBusiness.code` field's own Pydantic pattern (`^[0-9]{5}$`) rejected any code containing the underscore — meaning a doctor, sports manager, or event manager correctly selecting their actual, official business-nature code would have that row either silently filtered out of the mapped list, or (if constructed directly) fail Pydantic validation outright.

**What was fixed:**
- `app/schemas/itr3.py`: `NatureOfBusiness.code`'s pattern widened to `^[0-9]{5}(_[0-9]+)?$`; added the missing `trade_name` field.
- `app/engine/draft_to_itr3_input.py`: added a shared `_NATURE_OF_BUSINESS_CODE` regex (`[0-9]{5}(_[0-9]+)?`) replacing the `.isdigit()` filter in both mapper paths (workspace raw-JSON and canonical `draft.itr3NatureOfBusiness`), and wired `trade_name` through both.
- `app/engine/itd/itr3.py`: `_parta_gen2()`'s nature-of-business row construction rewritten to emit `TradeName1` when present, and — a related correctness fix caught while touching this code — `Description` is now omitted (not emitted as `""`) when absent, since the schema's own `nonEmptyString` constraint on that field means an empty string would have failed official schema validation the moment a row had no description at all.

**Verification**: two new tests — `test_itr3_nature_of_business_trade_name_reaches_json` and `test_itr3_nature_of_business_disambiguated_codes_not_silently_dropped` (all three `_N`-suffixed codes survive the mapper and reach the JSON) — both validated against the official `NatOfBus` schema sub-definition and confirmed genuinely failing pre-fix via `git stash`. The pre-existing `test_itr3_builder_maps_parta_gen2_workspace` needed a one-line update to its own expected output, since its existing test data (`tradeName="Tech"`) had been silently exercising the bug without asserting on it. ITR-3 suite: 66 passed (up from 64). Full combined regression: 1321 passed, same 10-failure baseline + the one already-flagged unrelated ITR-2 regression — no new failures.

### Schedule 5 — Part A-BS (Balance Sheet) — ✅ closed 2026-09-17

**What was found**: this is the schedule the earlier status investigation flagged as the biggest known gap, and it was — the typed `BSOfficial` tree was missing five entire required sub-blocks the schema demands with a `default: 0` (meaning they must always be present, unlike a genuinely optional disclosure): **`Investments`** (long-term + trade investments, form item 2), **`MiscAdjust`** (misc. expenditure / deferred tax asset / accumulated losses, form item 4), **`LoanAdv`** (loans and advances, form item 3b), **`NetCurrAsset`** (form item 3e — explicitly documented in the schema as "can be negative"), and **`NoBooksOfAccBS`** (form item 6, the entire "no account case" alternative). Directly confirmed live: a minimal draft with no balance-sheet data at all produced a `PARTA_BS` failing official schema validation with over a dozen missing-required-property errors (`FundApply.Investments`, `FundApply.MiscAdjust`, `CurrAssetLoanAdv.{CurrAsset,LoanAdv,CurrLiabilitiesProv,NetCurrAsset}`, `FixedAsset.{GrossBlock,Depreciation,NetBlock,CapWrkProg}`, `FundSrc.{LoanFunds,DeferredTax,Advances}`, `PropFund.{PropCap,ResrNSurp}`) — the codebase's own `itr3_preparation.py` had deliberately excluded this schedule from its "complete" list for exactly this reason.

Two further real bugs surfaced while closing this out, neither previously caught because a full schema validation of this schedule had apparently never actually been run before this push:
1. **`BSLoanGroup` (secured loans) had the wrong shape, not just missing fields.** The schema nests `FrmBank`/`FrmOthrs`/`TotRupeeLoan` under a `SecrLoan.RupeeLoan` sub-object; the typed model had them as flat siblings of `ForeignCurrLoan`/`TotSecrLoan`. Every secured-loan disclosure this codebase has ever emitted would have failed live schema validation with "Additional properties are not allowed" plus "'RupeeLoan' is a required property" — hidden until now behind the more obvious missing-block failures above.
2. **`TotLoanFund`** (`FundSrc.LoanFunds.TotLoanFund`, required) was being populated via `dict.pop("TotLoanFund", 0)` against a dict that could never actually contain that key (no typed field produced it) — always silently `0`, regardless of the real secured+unsecured loan totals.
3. **`PropCap`/`TotPropFund`** (proprietor's capital) had an incorrectly-strict `ge=0` constraint — the official schema places no minimum on these two fields specifically, a real, expected case for a business whose accumulated losses exceed contributed capital.

**What was fixed:**
- `app/schemas/itr3.py`: added `BSLongTermInv`, `BSTradeInv`, `BSInvestments`, `BSLoanAdvances`, `BSMiscAdjust`, `BSNoBooks`, and `BSRupeeLoan` (the last replacing `BSLoanGroup`'s flat fields with the correctly-nested shape); wired all seven into `BSOfficial`; removed the incorrect `ge=0` from `BSProprietorsFund.capital`/`.total`.
- `app/engine/draft_to_itr3_input.py`: `_balance_sheet()` extended to extract every new sub-block from the raw workspace JSON path. **The legacy fallback path (`draft.itr3BalanceSheet`, the simple flat summary model used when no rich workspace exists) previously never constructed a `BSOfficial` at all** — `official` was always `None`, silently routing every such filer through the builder's most minimal (and, per above, most broken) code path. Now builds a complete `BSOfficial` there too, placing the flat aggregate values (no finer breakdown exists in this simpler model) into each sub-group's own total field, and wires in the already-existing-but-never-used `noBooksOfAccounts`/`noBooksSundryDebtors`/etc. fields.
- `app/engine/itd/itr3.py`: `_parta_bs()` rewritten — the old minimal fallback branch (reachable whenever `official is None`) is now unreachable in practice (both mapper paths guarantee `official` is populated) and replaced with a fail-closed `ValueError` guard rather than silently emitting an incomplete tree; `Investments`, `LoanAdv`, `MiscAdjust`, `NetCurrAsset`, and `NoBooksOfAccBS` (when present) are all now popped/emitted for real; `TotLoanFund` is now genuinely computed as `secured_total + unsecured_total`.

**Verification**: four new tests — `test_itr3_balance_sheet_minimal_draft_now_schema_valid` (the exact minimal-draft case that failed live during the earlier status investigation, now validated clean against the full official `PARTA_BS` schema definition, not just a scoped sub-check), `test_itr3_balance_sheet_workspace_investments_and_misc_adjust_reach_json` (every new sub-block, real values, through to the JSON), `test_itr3_balance_sheet_no_books_case_reaches_json` (form item 6), plus the pre-existing `test_itr3_mapper_prefers_workspace_balance_sheet_over_legacy_fields` continuing to pass unchanged. Three of the four confirmed genuinely failing pre-fix via `git stash`. ITR-3 suite: 69 passed (up from 66). Full combined regression: 1324 passed, same 10-failure baseline + the one already-flagged unrelated ITR-2 regression — no new failures.

### Schedule 6 & 7 — Part A-Manufacturing Account / Part A-Trading Account — ✅ closed 2026-09-17

Closed together: both schedules share one mapper function (`_business_accounts()`), one pair of sibling schema classes, and are architecturally a matched pair on the official form itself (Manufacturing Account's own output, form item 3 "Cost of Goods Produced", feeds directly into Trading Account's item 11 "Cost of goods produced — Transferred from Manufacturing Account"; both share one combined header instruction on adjacent PDF pages).

**What was found**: contrary to the tracker's own prior-finding note ("generic key-lookup passthrough, not individually typed"), the *frontend* was already fully built out — `ITR3BusinessCoreManager.tsx` is a dedicated, official-schema-driven editor (not a generic fallback) covering every scalar, nested object, and repeatable-row field for both schedules, including calculated read-only totals. The generic-passthrough problem was entirely on the *backend*:
1. **`ManufacturingAccount`/`TradingAccount` (`app/schemas/itr3.py`) were untyped `dict[str, Decimal]` bags**, not individually typed models — no nested structure, no array support, nothing schema-checkable at the Python level.
2. **The mapper's `_decimal_map()` helper silently dropped every nested object** (`if not isinstance(item, Mapping)`) when flattening the raw workspace JSON — meaning `TradingAccount.ExciseCustomsVAT` (duties on sales, form item 4C) and `TradingAccount.DutyTaxPay.ExciseCustomsVAT` (duties on purchases, form item 10) were **completely discarded** for every filer who used them, regardless of what was entered in the (already-correct) frontend editor.
3. **Both official array fields were hardcoded to `[]`** in the builder — `OtherOperatingRevenueDtls` (form item 4A.iii, itemized "other operating revenue" rows) and `OtherIncDtls` (form item 9iii, itemized "other direct expenses" rows, per the form's own note "row can be added as per the nature of Direct Expenses") — any itemized entry a taxpayer added via the frontend's own repeatable-row editor was silently dropped before reaching the JSON.
4. **A dead lookup key**: the old builder read `amount("DutyTaxPay.TotExciseCustomsVAT")` — a literal dotted string — against a flat dict that could never contain that exact key (the mapper never produced dotted keys either), so this fallback could never match anything, on top of bug 2 already discarding the source data.
5. **Both schedules were unconditionally emitted as zero-filled stub objects** in every filing, even for a filer with no manufacturing/trading business at all (e.g. a pure-services professional) — never omitted, unlike every other genuinely optional ITR-3 schedule.

**What was fixed:**
- `app/schemas/itr3.py`: replaced both dict-bag classes with fully typed models matching the official schema exactly — `MfgOpeningInventory`/`MfgClosingStock` (Manufacturing Account, form items 1-2, all `ge=0` per the schema's own `minimum: 0` on every leaf) and a `ManufacturingAccount` wrapper; `TradingOtherRevenueEntry`/`TradingOtherIncomeEntry` (the two array row types), `TradingExciseCustomsVAT`, `TradingDutyTaxPayExciseCustomsVAT`/`TradingDutyTaxPay`, and a fully-typed `TradingAccount` (form items 4-12d). Four fields deliberately carry **no** `ge=0` constraint because the official schema itself places no minimum on them: `ManufacturingAccount.cost_of_goods_produced` (item 3, "1F − 2") and `TradingAccount.GoodsCostPrdcdFrmMA`/`GrossProfitFrmBusProf`/`IncomeIntradayTrd`/`IncomeFutureTrd` (items 11/12/12b/12d — a trading, intraday, or F&O result can be a genuine loss).
- `app/engine/draft_to_itr3_input.py`: `_business_accounts()` rewritten to walk the nested workspace structure explicitly (matching the established `_balance_sheet()`/`_audit_info()` idiom — nested `Mapping` traversal + `_decimal_value()` per leaf, not a blind flatten), including both array fields (filtered to rows with a real name, matching every other array mapper in this codebase). The now-unused generic `_decimal_map()` helper was deleted rather than left as dead code.
- `app/engine/itd/itr3.py`: `_manufacturing_account()`/`_trading_account()` rewritten to read the typed nested model directly (no more string key lookups, dotted or otherwise); both now return `{}` when the taxpayer has no data for that schedule at all (`account is None`), instead of a zero-filled stub — wired into the builder's existing "omit empty optional schedule" cleanup loop (`ManufacturingAccount`/`TradingAccount` added to the same tuple `ScheduleDPM`/`ScheduleGST`/etc. already use for this exact pattern).
- Also fixed, in the same test file, a **latent bug in ITR-3's own test suite** found immediately adjacent to this work: a test function was missing its `def test_...` line (evidently dropped during an earlier edit), causing its body to execute unconditionally as top-level module code at import time rather than run as an actual pytest test — meaning it contributed nothing to the ITR-3 test count despite appearing to. Restored as `test_itr3_mapper_prefers_workspace_audit_and_business_over_legacy_fields`; its assertions (already-passing) confirmed genuinely running as a real test post-fix (31 → 32 passed with no other change).

**Verification**: three new tests — `test_itr3_trading_account_nested_duties_and_other_revenue_rows_reach_json` (the two nested duty objects and both array fields, mapper through to JSON), `test_itr3_manufacturing_and_trading_full_schema_valid` (a fully-populated pair validated against the complete official `ManufacturingAccount`/`TradingAccount` schema definitions, not a scoped sub-check — this one passed even pre-fix, since dropping data silently doesn't make the *remaining* zero-defaulted output schema-invalid; a confirmation test, not a bug-catching one, same category as Schedule 3's `..._not_fabricates` test), and `test_itr3_manufacturing_and_trading_omitted_when_no_data` (both schedules absent from the JSON entirely for a filer with no such business). The first and third confirmed genuinely failing pre-fix via `git stash`. ITR-3 suite: 73 passed (up from 69). Full combined regression: 1328 passed, same 10-failure baseline + the one already-flagged unrelated ITR-2 regression — no new failures.

### Schedule 8 — Part A-P&L — ✅ closed 2026-09-18

**What was found**: the tracker's own prior-finding note ("totals real; dozens of `DebitsToPL` line items hardcoded 0") was verified directly and is accurate, but it understated the scale. Re-deriving `definitions["PARTA_PL"]` fresh from `Reference Docs by CBDT & ITD/Official JSON Schema/ITR-3_2026_Main_V1.1 (2).json` gives **193 leaf fields** across 6 required top-level blocks (`CreditsToPL`, `DebitsToPL`, `TaxProvAppr`, `NoBooksOfAccPL`, `TurnverFrmSpecActivity`, `NetIncomeFrmSpecActivity`) plus the optional presumptive/non-resident sections. The pre-fix typed model (`ProfitAndLoss` + `PLOtherIncome` + `PLInterestExpense`) covered roughly 21 of them. Concretely, before this fix:

1. **Every ordinary debit category was hardcoded to `0`** in the builder — form items 16-49 (freight, consumption of stores, power and fuel, rents, repairs, the whole `EmployeeComp` block, all four `Insurances`, staff welfare, entertainment, hospitality, conference, sales promotion, advertisement, the three commission/royalty/professional two-sided breakdowns, hotel/boarding, travel, foreign travel, conveyance, telephone, guest house, club, festival, scholarship, gift, donation, rates and taxes, audit fee, provisions). A taxpayer with real expenditure there filed a P&L showing zero.
2. **All three commission/royalty/professional groups shared one `_obj` dict** (`{"NonResOtherCompany": 0, "Others": 0, "Total": 0}`), so even the shape could not distinguish them.
3. **Every optional detail row was dropped**: `CreditsToPL.OthIncome.OtherIncDtls` (form item 14xi a/b — the itemized "any other income"), `DebitsToPL.OtherExpensesDtls` (item 46), `BadDebtDtls.BadDebtAmtDtls` (item 47i, PAN/Aadhaar bad-debt rows), and `BadDebtDtls.OthersPANNotAvlblDtl` (item 47ii, name-and-address bad-debt rows). `BadDebt`, both bad-debt totals, and `OthersAmtLt1Lakh` were likewise always `0`.
4. **The tax-provision/appropriation tail was hardcoded** — `BalBFPrevYr` (item 57), `AmtAvlAppr` (58), `TrfToReserves` (59), `ProprietorAccBalTrf` (60) were all `0` regardless of real data.
5. **The entire no-books alternative was hardcoded** — the payment-mode splits (item 64(i)(a)1/2, 64(ii)(a)1/2) and the whole profession sub-block (`GrossReceiptPrf`/`GrossProfitPrf`/`ExpensesPrf`/`NetProfitPrf`/`TotBusinessProfession`) were `0`.
6. **All presumptive and non-resident sections were absent entirely**: `NatOfBus44AD/44ADA/44AE` rows, `PersumptiveInc44AD`/`PersumptiveInc44ADA` (items 61/62), `GoodsDtlsUs44AE` (item 63 vehicle rows), `TotalNumOfMonths`/`TotalPrsumptvIncUs44EGoods`/`TotalPrsumptvIncUs44E`, and `NonResidentPL`/`NonResidentPLDetails` (item 66, sections 44B/44BB/44BBA/44BBC/44BBD).
7. **Three `OthIncome` fields were hardcoded**: `LiabilityWrittenBack`, `AmtofInterest`, `AmtofRem`.
8. **The typed model had no nested structure at all** for any of the above — `ProfitAndLoss` was a flat set of ~21 scalars with two small sub-models, not the official nested tree.

As with Schedules 1-7, the *frontend* already captured the whole official tree: `ITR3PartAFinancialEditor` is a dedicated official-schema-driven editor for `PARTA_PL` covering every scalar and nested path, backed by the `ITR3_PARTA_PL_FIELDS` manifest in `frontend/src/domain/itr3PartAFinancials.ts`, and `ITR3PresumptiveManager` covers the presumptive rows. **No frontend/draft-schema changes were needed** — purely a backend schema/mapper/builder gap.

**What was fixed:**
- `app/schemas/itr3.py`: replaced the flat summary model with the full official typed tree — `PLPLPLPLPartACreditsToPLOthIncomeOtherIncDtlsRow`, `PLPLPLPartACreditsToPLOthIncome`, `PLPLPLPartACreditsToPL`, `PLPLPLPartADebitsToPLEmployeeComp`, `PLPLPLPartADebitsToPLInsurances`, the three two-sided `...CommissionExpdrDtls`/`...RoyalityDtls`/`...ProfessionalConstDtls` groups, `PLPLPLPLPartADebitsToPLRatesTaxesPaysExciseCustomsVAT`/`PLPLPLPartADebitsToPLRatesTaxesPays`, `PLPLPLPartADebitsToPLOtherExpensesDtlsRow`, `PLPLPLPLPartADebitsToPLBadDebtDtlsBadDebtAmtDtlsRow`, `PLPLPLPLPartADebitsToPLBadDebtDtlsOthersPANNotAvlblDtlRow`, `PLPLPLPartADebitsToPLBadDebtDtls`, `PLPLPLPartADebitsToPLInterestExpdrtDtls`, `PLPLPLPartADebitsToPL`, `PLPLPartATaxProvAppr`, `PLPLPartANatOfBus44ADRow`/`PLPLPartAPersumptiveInc44AD`/`PLPLPartANatOfBus44ADARow`/`PLPLPartAPersumptiveInc44ADA`/`PLPLPartANatOfBus44AERow`/`PLPLPartAGoodsDtlsUs44AERow`, `PLPLPartANoBooksOfAccPL`, `PLPLPartANonResidentPLDetailsRow`/`PLPLPartANonResidentPL`, and a `PLPartA` wrapper — all with official aliases, `ConfigDict(populate_by_name=True, extra="forbid")`, `Decimal` money, and per-field official `minimum`/`maximum` bounds (e.g. `GrsTrnOverOrReceipt` ≤ ₹3cr for 44AD, `GrsReceipt` ≤ ₹75L for 44ADA, `TonnageCapacity` 0-100, `HoldingPeriod` 1-12, `PresumptiveIncome` ≥ 7500, `PinCode` 100000-999999, `Section` restricted to the official 44B/44BB/44BBA/44BBC/44BBD enum). Fields the schema deliberately leaves unrestricted keep no lower bound — `GrossProfit`, `PBIDTA`, `PBT`, all seven `TaxProvAppr` fields, `NoBooksOfAccPL.GrossProfit`/`GrossProfitPrf`, `ProvForBadDoubtDebt`, `OthProvisionsExpdr`, and the five `OthIncome` profit/gain leaves (`ProfitOnSaleFixedAsset`, `ProfitOnInvChrSTT`, `ProfitOnOthInv`, `ProfitOnCurrFluct`, `ProfitOnCnvInvntryToCapAsst`) plus `LiabilityWrittenBack` and `MiscOthIncome`, since a genuine loss or negative balance is a real value there. A machine diff of the typed model's aliases against the official definition now reports **193 official leaf fields, 193 implemented, 0 missing, 0 extra**.
- `app/engine/draft_to_itr3_input.py`: `_profit_and_loss()` rewritten to validate the whole official workspace tree through `PLPartA.model_validate(...)` (after a `_normalize_parta_pl()` pass that converts every numeric leaf to `Decimal` and preserves the nested/array shape), then derive the legacy convenience scalars from that typed tree. Every field and every array row is now carried, not just the previous summary subset.
- `app/engine/itd/itr3.py`: `_parta_pl()` rewritten to serialize the typed tree via `model_dump(by_alias=True, exclude_none=True)` through `_official_integer_tree()`, with the four typed summary scalars authoritative, and `_parta_pl_defaults()` retained to fill only the schema-required blocks/fields when no workspace data exists. Optional blocks and optional arrays are now genuinely **omitted** when absent (never fabricated), while required-with-default fields still always emit.
- **Two real bugs found and fixed in the subagent-authored intermediate state, both caught by running the official schema rather than trusting the diff**: (1) `GrsTrnOverstrOthMode`/`GrsTrnOverstrOthMode44ADA` had been given self-referential aliases, so the real official field names `GrsTrnOverAnyOthMode`/`GrsTrnOverAnyOthMode44ADA` would have failed `extra="forbid"` and rejected every real 44AD/44ADA return; (2) three **unrelated** PARTA_QD row fields (`ITR3TradingQDRow`/`ITR3RawMaterialQDRow`/`ITR3FinishedProductQDRow`) had been renamed from the correct official `AnyShortExces` to `strShortExces`, breaking three previously-passing `tests/test_itr3_parta_oi_qd.py` tests. Both reverted/corrected, and the QD regression was confirmed by stash-testing that those three tests pass at the pre-Schedule-8 commit. **No ITR-3 validator was touched** — `app/engine/validators/itr3/parta_pl.py`'s existing pre-compute arithmetic check (`validate_parta_pl_arithmetic`) was left exactly as-is; the new test fixtures were instead made arithmetically consistent with its four official chains (credits = trading gross profit + total other income; PBIDTA = credits − expenditure; PBT = PBIDTA − interest − depreciation; PAT = PBT − current tax − deferred tax).

**Verification**: four new tests — `test_itr3_typed_parta_pl_nested_rows_preserve_arrays` (typed rows are real models and *every* row of a repeatable array survives, not a first-row-only flatten), `test_itr3_builder_emits_fully_populated_parta_pl_schema_valid_payload` (every block, nested object, detail array, bad-debt PAN row, bad-debt no-PAN address row, all three 44AD/44ADA/44AE presumptive sections, and the non-resident rows populated together, then validated against the complete official `PARTA_PL` definition with per-field value assertions), `test_itr3_parta_pl_optional_blocks_omitted_when_absent` (no fabricated optional blocks; required blocks still emitted; schema-valid), and `test_itr3_parta_pl_negative_capable_fields_preserved` (negative `ProfitOnCurrFluct`/`PBIDTA`/`PBT`/`GrossProfitPrf` reach the JSON unclamped and still schema-validate, while positive-only fields reject a negative). All four **confirmed genuinely failing pre-fix via `git stash`** (three with `assert 0 == <real value>` / `ImportError`, one with the optional block wrongly present) — none needed the "confirmation test" caveat. ITR-3 suite: 76 passed (up from 73). Full combined regression: **1331 passed, exactly the same 11 failures as the established baseline** (10 unrelated + the one already-flagged ITR-2 `_map_exempt_income` cross-form bug, still present and still unfixed) — zero new failures.

**Correction found during independent re-review (2026-09-18)**: the user asked to recheck whether Schedule 8 was implemented properly. Field-completeness was re-verified programmatically (walking the live schema and the actual `PLPartA.model_fields` tree by alias, not by eye) and confirmed genuinely accurate — 167 leaf paths (the earlier "193" in the commit message counts array *item* fields per-array rather than per-leaf-path; both counting conventions land on the same real field set, confirmed 0 missing / 0 extra either way). The bound-fidelity claim ("per-field official minimum/maximum bounds") was **not** fully accurate, though: an automated `ge`/`le`-vs-schema-`minimum`/`maximum` diff over every leaf found **13 mismatches across 6 fields** the manual review had missed — none data-loss bugs (field completeness was never affected), but real gaps in how permissive the local Pydantic validation is relative to the official schema:
- `PersumptiveInc44AD.TotPersumptiveInc44AD`, `PersumptiveInc44ADA.TotPersumptiveInc44ADA`, `NonResidentPL.NetProfit`, `TotalNumOfMonths`, `TotalPrsumptvIncUs44E`, `TotalPrsumptvIncUs44EGoods` were all missing their schema-mandated `minimum: 0` floor (and `TotalNumOfMonths` was also missing its `maximum: 120` ceiling) — a doc comment on the first of these claimed "no official minimum," which the schema JSON directly contradicts. A value violating these floors/ceiling would have passed local construction and only failed at live/official schema validation, later than this codebase's own fail-closed discipline calls for.
- `NoBooksOfAccPL.GrossProfit`/`GrossProfitPrf`/`TotBusinessProfession` carried a fabricated `le=99999999999999` ceiling the schema does not place on any of the three (each is genuinely unbounded, like the sibling `PLPartA.GrossProfit`/`DebitsToPL.PBIDTA`/`PBT` fields the original fix correctly left unbounded) — overly *restrictive* relative to the schema, the opposite direction from the six fields above, but still a real fidelity gap.
- One further transcription slip caught while fixing these: an initial attempt to "correct" `TotPersumptiveInc44ADA`'s `le` from `9999999` (7 digits) to the standard 14-nines ceiling was itself wrong — the schema's actual maximum for this one field genuinely is the narrower `9999999`, confirmed by re-extracting it directly; reverted before landing.

All 13 fixed in `app/schemas/itr3.py`; a new regression test, `test_itr3_parta_pl_bounds_match_official_schema_exactly`, exercises all six previously-unbounded-floor fields (including the 120-month ceiling) plus the three previously-over-restricted `NoBooksOfAccPL` fields, confirmed genuinely failing pre-fix via `git stash`. Automated re-diff after the fix: **0 bound mismatches, 0 missing, 0 extra** across all 167 leaf paths. ITR-3 suite: 77 passed (up from 76). Full combined regression: 1332 passed, same 11-failure baseline — no new failures.

### Schedule 9 — Part A-OI (Other Information) — ✅ closed 2026-09-18

**What was found**: the tracker's own prior finding ("full passthrough") badly understated the severity. `app/schemas/itr3.py`'s `OIAmountGroup` was a single shared class reused for **all seven** of PARTA_OI's distinct sub-blocks (`AmtDisallUs36`, `AmtDisallUs37`, `AmtDisallUs40`, `AmtDisallUs40A`, `AmtDisall43B`, `AmtDisallUs43BPyNowAll`, `AmtExciseCustomsVATOutstanding`) — a ~48-field "kitchen sink" containing the *union* of every one of those seven blocks' own official field names. Since the builder (`_parta_oi()`) does a blind `model_dump(by_alias=True, exclude_none=True)`, every sub-block's JSON carried all 48 fields regardless of which section it actually represented. Directly confirmed live: validating a **completely default, empty-data** `ITR3PartAOI()` instance against the real official `PARTA_OI` schema definition produced **10 schema errors across all 7 sub-blocks** — `Additional properties are not allowed` for six of them, plus `'ExciseCustomsVAT' is a required property` / `0 is not of type 'object'` for the two blocks (`AmtDisall43B`, `AmtDisallUs43BPyNowAll`) that actually need a nested sub-object (`AmtUs43B{...}`), which the shared class instead modeled as a flat scalar. This means **every real 44AB-audited ITR-3 filer** (the schedule is mandatory in that case) whose return reached this block would have produced non-schema-valid JSON, regardless of what data was entered — this is not a "missing detail" gap, it is a complete structural break for 6 of 7 sub-blocks.

A second, independent gap: **every single numeric leaf field in the whole schedule (99 of them) carried no `ge`/`le` bound at all** — not a handful like Schedule 8's bound-fidelity gap, the entire schedule had none. All 99 leaves have an official `maximum: 99999999999999`, and all but three (`ProfDeviatDueAcctMeth`, `DecProOrIncLossUs145_2`, `PriorAmtIncCrDrPL` — items 3a/3b/15, whose "increase/decrease" form text admits a negative value) also carry `minimum: 0`.

A third gap, on the frontend: `frontend/src/domain/itr3PartAOI.ts`'s field list had **no entry at all** for `MethodOfAcct`/`ChangeInAcctMethFlg` (form items 1/2) or the whole `MethodOfValClgStk` group (item 4) — required schema fields with literally no UI path to set a non-default value — plus one genuinely broken duplicate entry: a stray `{key:'NoCredToPLAmt', group:'noCredToPLAmt', ...}` row that, if a user ever interacted with it, would write a bogus self-named `NoCredToPLAmt.NoCredToPLAmt` key into the real `NoCredToPLAmt` object (confirmed by tracing `updateItr3PartAOI`'s own object-branch logic) — silently corrupting that block the moment `extra="forbid"` was added. A second, harmless duplicate (`amountOfExpDisallwUs14A` vs. the correctly-cased `AmountOfExpDisAllwUs14A`) rendered the same field twice.

The calculator was confirmed to never read `parta_oi` at all — but this was judged **not a defect for this schedule**, matching the precedent already set by Manufacturing/Trading Account and Part A-P&L: Part A-OI is a disclosure schedule whose disallowance totals feed into Schedule BP's own computation (item 14, "6s of Part A-OI") via Schedule BP's own independently-entered workspace fields, not via direct calculator cross-reference from this schedule — that bridging is Schedule BP's concern (tracker row #14), not this one's.

**What was fixed:**
- `app/schemas/itr3.py`: replaced `OIAmountGroup` with seven correctly, individually typed classes — `AmtDisallUs36Group`, `AmtDisallUs37Group`, `AmtDisallUs40Group`, `AmtDisallUs40AGroup` (flat, matching each block's own real field set), `OIAmtUs43B` + `OIAmt43BGroup` (the correct nested shape, shared by both 43B blocks since the schema gives them an identical structure), `OIExciseCustomsVATOutstanding` + `OIExciseCustomsVATOutstandingGroup`. All seven, plus `OIStockValuation`/`OINoCredit`/`ITR3PartAOI` itself, now carry `extra="forbid"` (matching the Schedule 8 precedent) so a future field-name mismatch fails fast instead of silently misshaping the JSON. Added `ge=0, le=99999999999999` (or `le` only, for the three negative-capable fields) to all 99 leaves; `MethodOfAcct`/`ChangeInAcctMethFlg`/`ScheduleTPSAFlg`/`ValRawMaterial`/`ValFinishedGoods`/`ChngStockValMetFlg` typed as `Literal[...]` matching the schema's own enum lists exactly.
- `app/engine/draft_to_itr3_input.py` / `app/engine/itd/itr3.py`: **no changes needed** — both already used the generic `ITR3PartAOI.model_validate(oi_source)` / `parta_oi.model_dump(...)` pattern; fixing the schema shape alone fixes the whole mapper → builder path.
- `frontend/src/domain/itr3PartAOI.ts`: removed the broken duplicate `NoCredToPLAmt` self-referential entry and the harmless duplicate 14A entry; added real field entries (and a new `type:'select'`/`options` mechanism on `PartAOIField`, since every prior field was a plain money amount) for `MethodOfAcct`, `ChangeInAcctMethFlg`, `ScheduleTPSAFlg`, and all five `MethodOfValClgStk` sub-fields; extended `updateItr3PartAOI` to write string enum values (not `numeric()`-coerced) for the three string-typed nested keys; removed the dead, never-written `otherAdditions`/`otherDeductions` interface fields (not part of the official schema, confirmed never actually reaching stored workspace state).
- `frontend/src/components/ITR3PartAOIEditor.tsx`: renders a `<select>` for `type:'select'` fields instead of unconditionally rendering a number input.

**Verification**: backend — two new tests in `tests/test_itr3_parta_oi_qd.py`: `test_parta_oi_amount_groups_are_distinctly_typed_not_a_shared_dict` (a sibling block's field name is now rejected by each group's own `extra="forbid"`) and `test_parta_oi_full_schema_valid_with_distinct_per_block_data` (all seven sub-blocks populated with distinct, block-specific data, confirmed no cross-block field pollution, validated against the complete official `PARTA_OI` definition). Both confirmed genuinely failing pre-fix via `git stash`. A default-constructed `ITR3PartAOI()` now validates with zero schema errors (previously 10). An automated field-by-field diff against the live schema confirms **99/99 leaves present, 0 missing, 0 extra, 0 bound mismatches**. ITR-3 suite: 79 passed (up from 77). Full combined regression: 1334 passed, same 11-failure baseline — no new failures. Frontend: `npx tsc -b --noEmit` clean, `npm run build` succeeds, full Vitest suite 269/269 passing (one new test added, one existing test's stale wrong-cased key corrected). No ITR-3 validator touched; no ITR-1/2/4 code touched.

### Schedule 10 — Part A-QD (Quantitative Details) — ✅ closed 2026-09-18

**What was found**: smaller than Schedules 8/9, but with three real defects spanning schema, builder, and (most seriously) frontend:
1. **A frontend dead end**: `ITR3PartAQDEditor.tsx` — the component actually wired into `ITR3BusinessWorkspace.tsx` for this schedule (which deliberately excludes `PARTA_QD` from the generic `ITR3BusinessAuxiliaryManager` fallback that *does* support row add/remove) — only ever mapped over already-existing rows. It had no "Add row" control anywhere. For a taxpayer starting from a fresh return (`TradingConcern`/`RawMaterial`/`FinishrByProd` all empty/undefined), there was **no way to add a single quantitative-detail row through the UI at all** — the schedule was completely unreachable in practice, despite the backend mapper/builder being otherwise correctly wired.
2. **A confirmed schema-validation-breaking builder bug**: the official schema requires `ManfactrConcern` to carry *both* `RawMaterial` and `FinishrByProd` whenever it is present at all (`"required": ["RawMaterial", "FinishrByProd"]`) — there is no such thing as a manufacturing-concern disclosure with only raw-material rows or only finished-product rows on the real form. The builder's `if source.RawMaterial or source.FinishrByProd:` check emitted `ManfactrConcern` with whichever *one* side actually had data, omitting the other required key entirely. Directly confirmed live: a draft with `RawMaterial` rows but no `FinishrByProd` rows produced `'FinishrByProd' is a required property` against the real schema validator.
3. **No bound/enum fidelity at all**, matching the gap already found and fixed in Schedules 8/9: none of the three row types (`ITR3TradingQDRow`/`ITR3RawMaterialQDRow`/`ITR3FinishedProductQDRow`) had `ge`/`le` bounds, an `ItemName` length cap, or a `UnitOfMeasure` enum constraint — `UnitOfMeasure` accepted any string rather than being restricted to the official 23-value unit-code list (101-122, 999), so a typo'd unit code would pass local construction and only be caught later (if at all) by full-document schema validation.

**What was fixed:**
- `app/schemas/itr3.py`: all three row classes now carry `extra="forbid"`, `ItemName: str = Field(min_length=1, max_length=25)` (matching the schema's own `nonEmptyString`/`maxLength: 25`), `UnitOfMeasure` typed as a `Literal` of the exact 23-value official enum, and `ge=0, le=99999999999999` on every quantity field except `AnyShortExces` (no official bound — a signed shortage/excess figure, per the form's own "shortage/excess" wording) — confirmed 0 missing/0 extra/0 bound mismatches across all three row types via the same automated schema-diff method used for Schedules 8-9.
- `app/engine/itd/itr3.py`: `_parta_qd()` now raises `ValueError` naming the missing branch when `RawMaterial`/`FinishrByProd` are supplied asymmetrically, instead of silently emitting an incomplete, schema-invalid `ManfactrConcern` block.
- `frontend/src/domain/itr3PartAQD.ts`: added `addItr3PartAQDRow()`/`removeItr3PartAQDRow()` (respecting the schema's own 20-row-per-array cap), and `frontend/src/components/ITR3PartAQDEditor.tsx` now renders "+ Add row"/"Remove" controls per branch — the schedule can now actually be populated through the UI.

**Verification**: backend — two new tests in `tests/test_itr3_parta_oi_qd.py`: the existing `test_parta_qd_rejects_unknown_unit_code` strengthened to assert rejection at Pydantic construction time (not just downstream schema validation), and a new `test_parta_qd_manufacturing_concern_requires_both_raw_material_and_finished_product` (raw-only and finished-only both raise naming the missing side; both-present validates clean against the full official definition). Both confirmed genuinely failing pre-fix via `git stash`. Frontend — two new tests in `itr3PartAQD.test.ts` (add/remove round-trip, independent per-branch addition, 20-row cap + out-of-range removal rejection). ITR-3 suite: 80 passed (up from 79). Full combined regression: 1335 passed, same 11-failure baseline — no new failures. Frontend: `tsc` clean, `npm run build` succeeds, Vitest 272/272 passing. No ITR-3 validator touched; no ITR-1/2/4 code touched.

### Schedule 12 — Schedule S (Income from Salary) — ✅ closed 2026-09-18

**What was found**: the per-employer gross-salary breakdown, perquisite/profit-in-lieu/salary-nature detail arrays, and the 89A retirement-benefit-account block were all already correctly wired (confirmed by reading `_schedule_s()` in full before assuming a gap). Two entire, real sub-blocks were missing outright:
1. **`Section10_13A`** (form item 3's dedicated HRA sub-schedule, section 10(13A)) — never built at all. The underlying facts (`hra`, `rentPaid`, `isMetroCity`, `basic`/`da`) are captured per-employer on the shared `Employer` draft model and already correctly feed the *calculator's* own HRA exemption (`app/engine/draft_to_itr1_input.py::_map_salary`, shared by every ITR form via `compute_hra_exemption`) — but nothing surfaced that computation into the JSON disclosure ITD actually expects.
2. **`AllwncExemptUs10.AllwncExemptUs10Dtls`** (item 3's own itemized "which Section 10 sub-clause" breakdown array) — also never built, despite the calculator already computing the exact per-exemption amounts (`SalaryResult.gratuity_exempt`/`leave_encashment_exempt`/`retrenchment_exempt`/`vrs_exempt`/`transport_exempt`/`children_education_exempt`/`hostel_exempt`/`uniform_allowance_exempt`/`lta_exempt`) and the frontend (`EmployerEntryManager.tsx`'s own "Section 10 Exemption" dropdown+amount row editor) already capturing a taxpayer's own itemized claims per employer.

Both gaps had an exact, live-UAT-hardened precedent already shipped in `app/engine/itd/itr2.py::_schedule_s()` — ITR-2 and ITR-3 share the identical `ScheduleS` schema shape and the identical `SalaryResult` calculator dataclass (`app/engine/schedules/salary.py`), so the fix ports ITR-2's already-proven logic rather than re-deriving it. Notably, ITR-2's own code carries a hard-won, live-verified lesson worth preserving: **`Section10_13A` must be omitted entirely (not emitted as an all-zero placeholder) unless a genuine nonzero HRA exemption exists** — a real ITD Type-2 UAT call (2026-09-14, PAN GOYPT2026A) rejected a new-regime return purely because the block's mere *presence* signaled an active 10(13A) claim (`errCd ITR2_INF25_...`), regardless of the amounts inside it being zero.

Confirmed via the frontend (`EmployerEntryManager.tsx`, wired unconditionally for every ITR form including ITR-3 via `ITRComputationPage.tsx`'s shared `SalaryTab`) that `rentPaid`/`isMetroCity`/`section10ExemptionRows` are already fully captured per employer — **no frontend changes were needed**, purely a backend schema/mapper/builder gap.

**What was fixed:**
- `app/schemas/itr3.py`: `ITR3ScheduleSEmployer` gained `rent_paid`, `is_metro_city`, and `section10_exemption_rows` (the last mirroring the existing `salary_nature_rows`/`perquisite_nature_rows` list-of-dict pattern already used for the other detail arrays on this row).
- `app/engine/draft_to_itr3_input.py`: maps the three new fields from `draft.employers[].rentPaid`/`.isMetroCity`/`.section10ExemptionRows`.
- `app/engine/itd/itr3.py`: `_schedule_s()` now builds `AllwncExemptUs10Dtls` from the calculator's own per-exemption `SalaryResult` fields (via a local `_ITR3_SALARY_EXEMPTION_ROWS` tuple, ported verbatim from ITR-2's own field-to-code mapping) plus any frontend-captured section-10 rows, failing closed on the schema-illegal "OTH" catch-all code exactly as ITR-2 does; and builds `Section10_13A` by aggregating HRA/rent/salary facts across employers and calling `app.engine.common.hra.compute_hra_exemption()` directly (cleaner than re-deriving the formula inline, and guarantees the disclosure can never drift from the actual computation) — cross-checked against the calculator's own `sal.hra_exempt` (raises on mismatch), raises on mixed metro/non-metro employers (an ambiguous case the single aggregate block can't represent), and — porting ITR-2's live-UAT lesson — omits the block entirely rather than emitting it zero-filled when no genuine HRA claim exists.

**Verification**: three new tests in `tests/test_itr3_canonical_foundation.py` — `test_itr3_schedule_s_hra_and_section10_rows_reach_json` (old-regime employer with real HRA/rent/metro facts and a frontend section-10 row; asserts the exact least-of-three HRA arithmetic and validates against the official `ScheduleS` definition), `test_itr3_schedule_s_omits_section10_13a_when_no_genuine_hra_claim` (a plain salary-only employer with no HRA data emits neither block — a confirmation test, since the blocks never existed pre-fix either; passed even pre-fix, correctly documented as such rather than forced to fail), and `test_itr3_schedule_s_rejects_oth_section10_exemption_code` (fail-closed check). The first and third confirmed genuinely failing pre-fix via `git stash`. ITR-3 suite: 83 passed (up from 80). Full combined regression: 1338 passed, same 11-failure baseline — no new failures. No ITR-3 validator touched; no ITR-1/2/4 code touched (ITR-2's builder logic was read and ported, not imported or modified).

### Schedule 13 — Schedule HP (House Property) — ✅ closed 2026-09-18

**What was found**: no existing test file referenced Schedule HP at all before this push (confirmed by grep), which explains why the most severe defect here — a genuine, reproducible **crash**, not just a wrong value — had never surfaced. Four real defects, found by validating a realistic co-owned property against the live official schema directly rather than trusting the builder's apparent completeness:

1. **A crash bug**: `AsseseeShareProperty` (the property's own ownership-share percentage, form item 1f's multiplier) is an official schema `"number"` field with `multipleOf: 0.01` — but the builder emitted `source.assessee_share_percent` as a raw `Decimal` object, unconverted. `jsonschema`'s `multipleOf` check computes `instance / dB` where `dB` is always a plain Python `float` (loaded from the schema JSON) — dividing a `Decimal` by a `float` raises `TypeError` outright. This meant **official schema validation would crash, not merely fail, for every single Schedule HP property this codebase has ever built** (the field is unconditionally emitted for every row, co-owned or not).
2. **A data-corruption bug**: `PercentShareProperty` (each co-owner's own share, same `multipleOf: 0.01` percentage type) was being run through `_official_integer_tree()`, which blindly converts every `Decimal` via `_to_rupees()` — a money-only whole-rupee rounding helper. A legitimate co-ownership split like 37.5% would have been silently rounded to a whole number, corrupting real ownership-share data.
3. **A schema-violation bug**: optional identity fields on `CoOwners`/`TenantDetails` (`PAN_CoOwner`/`Aadhaar_CoOwner`/`PANofTenant`/`AadhaarofTenant`/`PANTANofTenant`) were being emitted as literal JSON `null` when absent, rather than omitted — `_official_integer_tree()` passes `None` through unchanged, and these fields are typed `{"type": "string"}` in the schema with no `"null"` variant, so a present-but-null key fails validation.
4. **`PassThroghIncome`** (form item 2) was hardcoded to `0` unconditionally, discarding any real Schedule-PTI house-property-head income — even though `typed_input.pti_entries` is already mapped from the draft (confirmed by grep; no cross-schedule dependency on Schedule PTI's own not-yet-closed work, tracker row #47) and the form's own note explicitly calls for exactly this ("include ... Pass-through income referred to in schedule PTI").

All four had an exact, already-proven fix already shipped in `app/engine/itd/itr2.py::_schedule_hp()` (`float()` conversion for both percentage fields, a `{k:v for k,v in {...}.items() if v is not None}` filter for the optional identity fields, and CBDT rule #79's `PassThroghIncome` = HP-head `pti_entries` sum) — ported directly rather than re-derived, since ITR-2 and ITR-3 share the identical `ScheduleHP` schema shape.

**What was fixed:**
- `app/engine/itd/itr3.py`: `_schedule_hp()` now converts `AsseseeShareProperty`/`PercentShareProperty` via `float()`, filters `None`-valued optional keys out of every `CoOwners`/`TenantDetails` row instead of emitting them as `null`, and computes `PassThroghIncome` from `typed_input.pti_entries` filtered to `income_head == "HP"` (CBDT rule #79).
- `app/schemas/itr3.py`: `ITR3ScheduleHPProperty` gained `property_owner: Literal["SE","MI","SP","OT"]` and `property_type: Literal["L","D","S"]` (matching the official enums exactly — the draft's own `PropertyOwnerType`/`PropertyType` literals already only produce these values, confirmed by inspection, so this tightening rejects nothing legitimate), `ge=0,le=100` on `assessee_share_percent`, and `ge=0` on every money field the schema itself bounds at zero (`annual_lettable_value`/`rent_not_realized`/`local_taxes`/`standard_deduction`/`interest_on_loan`/`arrears_unrealised_rent`) — `annual_value_owned`/`income_of_hp` deliberately kept unbounded below, matching the schema's own lack of a minimum (a let-out property can show a genuine loss).

**A tooling limitation found and documented, not "fixed"**: `jsonschema`'s `Draft4Validator` `multipleOf` check has a known floating-point-precision quirk — for some two-decimal percentage values (e.g. `33.33`), `value / 0.01` lands on `3332.9999999999995` in IEEE-754 binary rather than exactly `3333`, producing a false-positive "not a multiple of 0.01" rejection on a value that genuinely is one. This affects this codebase's *local validation tooling* (and, by the identical `float()` pattern, ITR-2's own already-shipped code carries the same latent quirk for unlucky percentage values) — it is not a defect introduced by this fix, and no reliable Python-side workaround exists without a fragile epsilon comparison. Documented here rather than silently worked around; the new regression test deliberately uses `62.5`/`37.5` (still genuinely fractional, proving real precision preservation) instead of a value that happens to trip this specific tool quirk.

**Verification**: two new tests in `tests/test_itr3_canonical_foundation.py` — `test_itr3_schedule_hp_full_schema_valid_with_fractional_co_owner_share` (a co-owned, let-out property with a fractional ownership split and a tenant with no PAN/Aadhaar, validated against the complete official `ScheduleHP` definition, confirming both the percentage precision and the omitted-not-null identity fields) and `test_itr3_schedule_hp_pass_through_income_from_pti_entries` (two PTI entries, one HP-head and one OS-head; confirms only the HP-head amount reaches `PassThroghIncome`). Both confirmed genuinely failing pre-fix via `git stash` (the first with the exact pre-fix crash/rounding defects, the second with `0 == 15000`). ITR-3 suite: 85 passed (up from 83). Full combined regression: 1340 passed, same 11-failure baseline — no new failures. No ITR-3 validator touched; no ITR-1/2/4 code touched (ITR-2's logic was read and ported, not modified).

### Schedule 14 — Schedule BP (Business/Profession) — ✅ fully closed 2026-09-18

**This is the largest, most complex schedule in ITR-3** (Part A items 1-38, Part B items 39-42, Part C items 43-49, Part D, Part E — roughly 90 leaf fields across 5 sub-objects). A first pass earlier the same day closed the mandatory arithmetic bridge and marked the schedule ⛔ (partially closed), deferring six item clusters as "no data capture exists anywhere." **That premise was re-investigated and found incomplete**: the frontend's generic Schedule BP editor (`frontend/src/components/business/ITR3ScheduleBPEditor.tsx`, driven by the full 131-path official coverage manifest `frontend/src/domain/itr3CoverageManifest.ts`) already renders every one of these fields as a raw editable input, keyed by the exact official JSON field name, writing into the same `ITR3ScheduleBP` workspace blob the mapper already reads items 14-33 from — the earlier deferral was checking only whether the *typed* `BusinessIncome` Pydantic schema had a dedicated field, not whether the raw workspace capture (the actual, already-wired data path for this schedule) already had one. Given that, this second pass closed every remaining item as a **backend-only** change (schema + `compute_pgbp()` + mapper + builder) — no new frontend work was needed.

#### What was fixed in this closure (on top of the earlier same-day fixes)

1. **Items 3a-3g** (income/receipts credited to P&L but chargeable under another head — Salary/HP/CG/OS/115BBF/115BBG/115BBH) now genuinely **subtract** from taxable business income, not just disclose. This is a real correctness fix, not disclosure-only: this engine computes Salary/HP/CG/OS income independently from their own typed inputs, so failing to remove a P&L-credited amount that actually belongs to another head would double-tax it. 3di/3dii (dividend-vs-other split of 3d) are disclosed but not separately subtracted (3d/`OtherSources` is the figure actually subtracted; 3di/3dii are its own informational breakdown).
2. **Items 7a-7g** (expenses debited to P&L relating to another head) and **items 8a/8b** (expenses relating to exempt income, 8b disallowed u/s 14A) now genuinely **add back** into taxable business income (they should never have reduced it).
3. **Items 5a/5b/5c/5A** (exempt income credited to P&L — firm/AOP share, other exempt income; receipts not chargeable to tax at all) now genuinely **subtract**.
4. **Items 19/23/28/29/30** (MSME interest disallowance u/s 23 of the MSMED Act; any other s.28-44DA addition; s.35/35CCC/35CCD excess deduction; amounts disallowed in an earlier year u/s 40/u/s 43B now allowable) now flow into the actual computation, sourced directly from their own dedicated Schedule-BP workspace fields (`InterestDisAllowUs23SMEAct`, `OthItemDisallowUs28To44DA`, `DebPLUs35ExcessAmt`, `AmtDisallUs40NowAllow`, `AmtDisallUs43BNowAllow`) — matching exactly how items 14-18/20-22/24-25/27/31-32 already worked, not a Part-A-OI cross-reference (the earlier pass's assumption that these needed Part A-OI was itself based on an incomplete read of the form: item 23 has no Part-A-OI citation at all on the official form text, and items 19/28/29/30's own dedicated Schedule-BP fields make a Part-A-OI detour unnecessary).
5. **Items 4b/37a-37e/38** (Rule 7/7A/7B(1)/7B(1A)/8 composite agricultural-and-business income — tea/coffee/rubber growing-and-manufacturing) are now fully wired: the raw composite-activity profit (4b) is subtracted at item 6, and the taxpayer's own rule-adjusted taxable figure (37a-37e, entered directly since this engine does not re-derive the Rule 7A/7B/8 percentage formulas itself) is added back on top of item 36 to reach item A37; item 38 (the agricultural balance) is the difference. **`NetPLBusOthThanSpec7A7B7C` (A37) now genuinely differs from `NetPLAftAdjBusOthThanSpec` (item 36) when a composite activity is declared** — previously these two fields were unconditionally forced equal, which was only ever correct in the (common) case of no composite income.
6. **A genuine tax-computation bug in `total_business_income`/Part E** (`app/engine/schedules/business.py`): the calculator floored each of the three business sub-heads (non-speculative, speculative, specified u/s 35AD) at 0 **independently**, then summed the floored values — silently discarding a real non-speculative business LOSS instead of netting it against speculative/specified business INCOME, which the Act (and the form's own Part E table) requires. Fixed by computing `total_business_income` as `max(0, non_spec_signed + speculative_signed + specified_signed)` — item D's own literal formula (`A37+B42+C48`), which is always correct regardless of intra-head bookkeeping since addition doesn't care about sign — and by adding the actual Part E intra-head set-off computation (loss absorbed by speculative income first, then specified income, matching the form's row-ii-then-row-iii order) so the **remaining** loss (not the raw, un-netted loss) is what flows into Schedule CYLA's own cross-head absorption, and the speculative income Part E already consumed is not also offered to CYLA as a still-available target (which would have let the same rupee absorb two different losses). Proven a strict no-op in the common case (a single regular business, no speculative/specified income coexisting with a loss) by `test_pgbp_part_e_no_op_when_no_speculative_or_specified_income_available`.
7. **Part E (`BusSetoffCurrYr`) now shows the real intra-head set-off table** instead of being hardcoded all-zero, sourced directly from the same computation `compute_pgbp()` already performs for its own CYLA-feed purposes (not independently re-derived, avoiding any risk of the disclosure drifting from the actual cross-head loss handling).
8. **`SpecBusinessInc`/`SpecifiedBusinessInc` (items B42/C48) now disclose the genuinely signed figures**, not a GTI-floored value — the official schema places no minimum on either field (the form's own text: "if loss, take the figure to 6xix/7xix of Schedule CFL"), so a speculative/specified business loss was previously disclosed as a false 0.
9. **`AdditionUs28to44DA`/`DeductUs28to44DA`/`AddSec28to44DA`/`DedSec28to44DAOTDedSec35AD`** (items 40/41/44/45) now disclose the speculative/specified business's own s.28-44DA additions/deductions — fields the calculator already consumed (`speculative_additions`/`speculative_deductions`/`specified_business_additions`/`specified_business_deductions` on `BusinessIncome`) but the builder never disclosed.
10. **A pre-existing mapper bug found while re-deriving item 23/24's exact field mapping**: `other_additions` (feeding item 24, `AnyOthIncNotInclInExpDisallowPL`) was incorrectly sourced from item 23's own raw field name (`OthItemDisallowUs28To44DA`) instead of item 24's (`AnyOthIncNotInclInExpDisallowPL`) — meaning item 24 was silently reading item 23's data, and item 23 itself was permanently 0. Fixed as part of adding item 23's own genuinely separate field.

#### Design decisions made explicitly, not left implicit

- **Items 8b/19/23/28/29/30 are sourced from the Schedule-BP workspace's own dedicated fields, not from Part A-OI.** The earlier same-day pass assumed these needed a Part-A-OI cross-reference and deferred them specifically because `compute_pgbp()` doesn't read Part A-OI. Re-reading the official form found this assumption wrong for 5 of the 6 items (only item 8b's form text even cites "(16 of Part A-OI)"; the Schedule-BP JSON already has its own dedicated field for the same figure, matching the established pattern every sibling disallowance item 14-33 already uses) — so no Part-A-OI wiring was needed at all, and the calculator-extension risk the earlier pass was worried about never materialized.
- **Rule 7/7A/7B(1)/7B(1A)/8's own percentage-based agricultural-vs-taxable split is NOT re-derived by this engine.** The taxpayer enters their own already-computed rule-adjusted taxable figure (items 37a-37e) directly, matching how the Schedule-BP workspace's generic editor already exposes these as plain number inputs. This is consistent with the engine's existing depreciation-schedule precedent (item 12i is "the item 6 of Schedule-DEP" figure, entered directly since no block-of-assets depreciation engine exists yet) — a genuinely complex, rarely-applicable statutory computation is captured as taxpayer input, not re-implemented.
- **`DeductionUs35AD`/item 47 (specified business's own s.35AD deduction) remains a deferred, honestly-zero field** — no typed source exists anywhere (same as before this pass), and `specified_signed`'s own formula never assumed a nonzero value for it either, so this is self-consistent, not a newly-introduced gap.
- **Schedule CFL wiring for a speculative/specified business loss carry-forward remains out of scope** — fixing B42/C48's sign (finding 8 above) means a genuine speculative/specified loss can now reach the JSON correctly, but whether that loss is correctly picked up by Schedule CFL's own carry-forward table is Schedule CFL's own scope, not Schedule BP's; flagged here as a forward pointer for whichever pass closes Schedule CFL for ITR-3.

#### Verification

19 new tests added across three layers (direct `compute_pgbp()` unit tests proving the actual computation changes; `_schedule_bp()` builder tests with full official-schema validation proving the disclosure; one mapper-level test proving the raw Schedule-BP workspace JSON — exactly what the frontend's generic editor writes — reaches every new `BusinessIncome` field, including the nested `IncRecCredPLOthHeadDtls`/`ExpDebToPLOthHeadDtls`/`IncCredPL`/`ProfitFrmActCvrd` sub-objects). All 14 fix-dependent tests confirmed genuinely failing pre-fix via `git stash` (stashing only the five source files, keeping the new tests) — the 7 pre-existing tests in the same file were confirmed to keep passing unchanged against the pre-fix code, proving the new tests aren't accidentally testing something already true. ITR-3 suite + CYLA/BFLA: 127 passed (up from 113). Full combined regression: run separately, same pre-existing baseline failures only (ERI/portal-automation collection errors, unrelated to any ITR-3 code). No ITR-3 validator touched; no ITR-1/2/4 code touched.

---

### Schedule 14 (original same-day pass, superseded by the full closure above) — historical record

The section below is preserved for its own detailed fix log of the mandatory-arithmetic-bridge bugs found first; every item it deferred was subsequently closed by the pass documented above.

**Confirmed, non-trivial bugs found and fixed** (all disclosure-layer only — none change `IncChrgUnHdProftGain`/GTI/tax liability, proven by construction and locked in by a dedicated regression test):
1. **Item 10 and item 13 were set to the identical value**, silently double-counting the depreciation adjustment. Per the form's own formula, item 10 ("Adjusted profit or loss") = 6+9 (BEFORE depreciation); item 13 ("Profit or loss after adjustment for depreciation") = 10+11-12iii (AFTER). The builder computed one value (`non_spec + books_depr - it_depr`) and used it for both fields.
2. **Items 4a and 35 (the presumptive-income breakdown by section) were both hardcoded to 0** — despite `draft.businesses` (Presumptive44AD/44ADA/44AE rows) already carrying exactly this data, grouped by `.scheme`. Since item 6 ("Balance") explicitly subtracts item 4a, and item 36 explicitly re-adds item 35, leaving both at 0 meant the ENTIRE presumptive income (the single most common real-world ITR-3 scenario) was mis-disclosed as ordinary "regular business" profit (items 6/10/13/34) rather than correctly classified as presumptive income.
3. **`NetPLAftAdjBusOthThanSpec`/`NetPLBusOthThanSpec7A7B7C` (items 36/A37) were sourced from the GTI-floored `non_spec_net_income`** (always >=0) instead of the unclamped `non_spec_signed` — even though the official schema explicitly places no minimum on either field (a genuine business loss is a real, expected value here). A loss-making regular business was disclosed as a false 0 instead of its true negative figure.
4. **`DeemIncUs3380HHD80IA`** (item 21's own combined total — the form prints all nine 32AD/33AB/33ABA/35ABA/35ABB/40A(3A)/72A/80HHD/80-IA deemed-income sub-references as ONE single line, not nine rows) **was hardcoded to 0** even though the sum of its nine components was already being correctly computed for `total_additions`.
5. **`other_additions`/`other_deductions` on `BusinessIncome`** (items 24/31, "any other item of addition"/"any other amount allowable as deduction") **were accepted by the calculator's own `compute_pgbp()` call but never actually sourced from anywhere** in the mapper — silently, permanently 0 regardless of what a taxpayer entered into the Schedule-BP workspace object.

**What was fixed:**
- `app/schemas/itr3.py`: `BusinessIncome` gained `presumptive_44ad_income`/`presumptive_44ada_income`/`presumptive_44ae_income`.
- `app/engine/draft_to_itr3_input.py`: `_business_income()` now derives the three presumptive fields by grouping `draft.businesses` on `.scheme`, and reads `other_additions`/`other_deductions` from the same workspace object (`OthItemDisallowUs28To44DA`/`AnyOthAmtAllDeduct`) every sibling disallowance field already uses.
- `app/engine/itd/itr3.py`: `_schedule_bp()` rewritten to compute items 6/9/10/13/26/33/34/35/36 as an explicit, named sequential chain matching the form's own formulas exactly (not a single reused intermediate value), correctly separating the depreciation-adjustment step, subtracting-then-re-adding the presumptive total across items 4a/6/35/36, and using the unclamped `non_spec_signed` for items 36/A37/37f.

**What was investigated and deliberately deferred (not fixed), with the exact reason each is genuinely blocked:**
- **Items 3a-3g / 7a-7g** (income/expenses credited/debited to P&L but properly belonging to another head — salary/HP/CG/OS/115BBF/115BBG/115BBH) — **no data capture exists anywhere** on the draft model for "this P&L figure actually belongs to another head." Building this needs new frontend UI, not just a mapper/builder fix.
- **Items 5a-5d / 5A** (exempt income credited to P&L — share of firm/AOP income, other exempt receipts, non-chargeable receipts) — same: no data capture exists.
- **Item 8a / item 8b** (expenses related to exempt income) — item 8b has an exact same-document cross-reference available on the already-closed Part A-OI (`AmountOfExpDisAllwUs14A`, item 16), but wiring it was deliberately **not** done this pass: `compute_pgbp()` (the calculator) does not read Part A-OI at all, so adding it to the Schedule-BP *disclosure* without also adding it to the *calculator* would make items 34/36 diverge from what `total_biz`/GTI actually used — a worse defect than the current honest 0. The same reasoning applies to **items 19 (MSME interest disallowance), 23 (other 28-44DA additions), 28 (ESR excess deduction — also blocked on Schedule ESR, #19, not yet closed), 29 (section 40 now-allowable), and 30 (section 43B now-allowable)** — each has an exact Part-A-OI cross-reference available, each was left at 0 for the identical reason. Fixing these properly needs the calculator extended first, a larger and riskier change than this schedule's own disclosure-layer scope; flagged here as the clear next step for a dedicated follow-up.
- **Items 37a-37e / 38** (Rule 7/7A/7B/8 composite-income adjustments — tea/coffee/rubber growing-and-manufacturing, coffee curing) — no data capture exists anywhere; a genuinely niche business type. Item 37f (`IncomeOtherThanRule`) and item A37 (`NetPLBusOthThanSpec7A7B7C`) correctly equal item 36 throughout, matching the form's own "if rule 7A/7B/8 is not applicable, enter same figure as in 36" instruction — this part is honestly complete, not deferred.
- **`BusSetoffCurrYr` (Part E, the intra-head business-loss set-off table)** — confirmed the *actual* set-off arithmetic (which loss got absorbed by which income, and how much remains) already happens correctly and independently inside the CYLA/BFLA computation (`calculators/itr3.py` already passes `pgbp.non_spec_signed`/`speculative_signed`/`specified_signed` into CYLA's own loss-set-off input) — so this gap is **disclosure-only**, not a tax-computation defect. Populating it correctly needs reading CYLA's own internal per-head set-off breakdown, which risks producing a partially-correct, internally-inconsistent table if done without full confidence in CYLA's own structure; left exactly as before (all-zero) rather than risk a worse, half-right disclosure.
- **Item 12i/12ii (depreciation allowable under the IT Act) were confirmed already correct**, not a gap: `business.depreciation_it_act` is a genuine typed field, already wired to `DepreciationAllowUs32_1_ii`, matching the guide's own "(item 6 of Schedule-DEP)" citation — the taxpayer/UI enters the Schedule-DEP-computed figure directly since no block-of-assets depreciation *engine* exists yet (tracker rows #15-17), but the *field* itself was never the gap.

**Verification**: five new tests in `tests/test_itr3_business_deductions.py` — the two pre-existing `_schedule_bp` tests updated for the corrected item-10-vs-13 split (both were asserting the old, double-counted value); `test_schedule_bp_presumptive_income_breakdown_reaches_json` (a three-scheme draft, all of items 4a/35/`TotDeemedProfitBusUs` correct, full schema validation); `test_schedule_bp_presumptive_breakdown_does_not_change_total_income` (proves item 36 = item 34 + item 35viii internally, and exactly equals the calculator's own `pgbp.non_spec_signed` — the central safety proof that the presumptive re-allocation is neutral); `test_schedule_bp_loss_disclosed_as_negative_not_floored_to_zero` (a hand-constructed loss scenario, confirms items 36/A37/34 show the true negative figure while `IncChrgUnHdProftGain` stays correctly floored at 0 for GTI). The presumptive-breakdown and loss-disclosure tests confirmed genuinely failing pre-fix via `git stash`; the neutrality test legitimately passes in both states for a non-loss scenario (a guard against future regression, not a bug-catching test — documented as such). ITR-3 suite: 88 passed (up from 85). Full combined regression: 1343 passed, same 11-failure baseline — no new failures. No ITR-3 validator touched; no ITR-1/2/4 code touched.

**Recommended next step for whoever closes this schedule fully** *(superseded — see the "✅ fully closed 2026-09-18" section above; items 8b/19/23/28/29/30 turned out not to need Part A-OI at all, and Part E turned out to be intra-PGBP-head, computed BEFORE CYLA rather than derived from it)*.

---

### Schedules 15-18 — Schedule DPM / DOA / DEP / DCG (Depreciation) — ✅ closed 2026-09-18

**Grouped together** because all four share one typed backend source (`ITR3DepreciationSchedules`) and one root cause. The tracker's own prior finding — "no block-of-assets depreciation engine exists anywhere in the backend" — was investigated and found **incomplete, not wrong about the underlying problem**: a real, working depreciation engine already exists, just implemented client-side.

**What was found**: `frontend/src/components/business/ITR3BusinessAuxiliaryManager.tsx` is a genuine, already-shipped generic manifest-driven editor (the same architecture as Schedule BP's own `ITR3ScheduleBPEditor.tsx`) covering Schedule DPM/DOA/DEP/DCG plus ESR/UD/ICDS/GST/IF/10AA/80-IA/IB/IC/80RA/TPSA. Its own `recompute()` function already implements the full official block-of-assets arithmetic for DPM/DOA (WDV opening + additions − realizations → full/half-rate depreciable amount → depreciation at full/half rate → total depreciation → net aggregate depreciation → capital gain u/s 50 → closing WDV), matching the official Schedule DPM/DOA formulas item-for-item. **But this editor writes into `draft.itr3BusinessWorkspace.auxiliary["ScheduleDPM"/"ScheduleDOA"]`, while the mapper (`draft_to_itr3_input.py`) read exclusively from `draft.itr3BusinessWorkspace.depreciationSchedules`** — a separate, typed Pydantic field that is grepped zero times anywhere in the entire frontend. This is the identical "frontend dead-end" pattern already found and fixed for Schedule 10 (Part A-QD) earlier in this push: real, working UI, completely disconnected from the field the backend actually reads.

**A second, independent gap**: Schedule DEP ("Summary of depreciation") and Schedule DCG ("Deemed capital gains on sale of depreciable assets") are themselves *summaries* of Schedule DPM/DOA's own per-rate-block totals (confirmed against the official form text: Schedule DEP item 1a = Schedule DPM item 17(i), the 15% block's own "Net aggregate depreciation"; Schedule DCG item 1a = Schedule DPM item 20(i), the same block's own "Capital gains/loss under section 50" — a *different* field, despite Schedule DEP and DCG's JSON reusing the identical field names `DeprBlockTot15Percent`/etc for both). The frontend editor marks every Schedule DEP/DCG field `readonly` (implying "calculated") but its own `recompute()` never actually calculates them — nothing writes to them at all. They needed deriving from the mapped DPM/DOA data, which the mapper had never done either.

**A real tax-computation bug this closes as a side effect**: `app/engine/calculators/itr3.py` already reads `dcg_schedule.SummaryFromDeprSchCG.TotalDepreciation` and adds it into the generic STCG bucket (Schedule CG item A6e, "deemed short-term capital gains on depreciable assets") — but since `depreciation_schedules` was never populated, this was **silently, permanently zero for every real taxpayer who sold a depreciable asset during the year that triggered a block-of-assets gain u/s 50** — an understated STCG, not just a missing disclosure.

**What was fixed**:
- `app/engine/draft_to_itr3_input.py`: new `_depreciation_schedules(draft)` reads `draft.itr3BusinessWorkspace.auxiliary["ScheduleDPM"/"ScheduleDOA"]` (falling back to the typed `.depreciationSchedules` field first, in case it's ever populated by a future caller). Each of DPM's four rate blocks (15/30/40/45) and DOA's Land/Building(5/10/40)/FurnitureFittings/IntangibleAssets/Ships is only included in the typed model if it carries at least one genuinely nonzero value, so an explicitly all-zero block (e.g. one the taxpayer cleared out) is not mistaken for real data — *(corrected 2026-09-18, same day, see the Update note below: the original text here claimed the frontend "unconditionally zero-fills every block's full field set on every save, including untouched ones" — that was wrong; every leaf field the taxpayer leaves genuinely blank stays absent, not zero, and every required field is now defaulted to 0 in the mapper itself for exactly that reason)*. `ScheduleDEP`/`ScheduleDCG`'s own summary objects are derived directly from the just-mapped `ScheduleDPM`/`ScheduleDOA` typed objects' own `NetAggregateDepreciation` (DEP) / `CapGainUs50` (DCG) fields, per rate, matching the official form's item-by-item cross-references exactly. `draft_to_itr3_input()`'s `depreciation_schedules=` now calls this helper.
- No changes needed to `app/engine/itd/itr3.py`'s `_schedule_dpm`/`_schedule_doa`/`_schedule_dep`/`_schedule_dcg` builders or to `app/engine/calculators/itr3.py`'s DCG-STCG wiring — both were already correct, just permanently starved of real input.

**Verification**: `tests/test_itr3_depreciation_schedules.py`, 8 new tests — an untouched (all-zero) rate block is correctly omitted rather than mistaken for real data; Schedule DEP's summary is correctly derived from DPM/DOA's `NetAggregateDepreciation`; Schedule DCG's summary is correctly derived from the *different* `CapGainUs50` field (proving the two same-named-field summaries aren't confused with each other); Land/FurnitureFittings/IntangibleAssets/Ships all reach the typed model; a full end-to-end test validates all four schedules against the real official JSON schema; a taxpayer with no depreciable assets sees none of the four schedules in the generated JSON (no zero-filled stubs); and a dedicated test proves `ScheduleDCG`'s `TotalDepreciation` now actually reaches `result.capital_gains_income`, closing the silent-understatement bug. All 8 tests confirmed genuinely failing pre-fix via `git stash` (a clean `ImportError` on the new mapper function, not just failed assertions). ITR-3 suite: 118 passed (up from 102). No ITR-3 validator touched; no ITR-1/2/4 code touched.

**Flagged, not fixed, for the next schedule's own turn**: Schedule ESR (#19, next in PDF order) very likely shares this exact root cause — the mapper reads `draft.itr3BusinessWorkspace.core.get("ScheduleESR")`, but `ITR3BusinessAuxiliaryManager.tsx` (the same editor this fix already read in full) writes ESR into `.auxiliary["ScheduleESR"]`, not `.core`. Schedule ICDS and Schedule GST show the identical `core.get(...)`-only pattern in the mapper. This is a hypothesis to *verify*, not assume, when each of those rows' own turn comes — do not fix it as a side effect of this entry.

**Update (2026-09-18, same day): a second, independent bug found and fixed while verifying this closure was genuinely end-to-end, not just schema-valid for the exact scenarios the first pass happened to test.** Asked directly whether Schedules 15-18 were "implemented end-to-end," the honest answer required re-deriving the frontend's actual field-by-field write behavior rather than trusting the first pass's own summary of it — which turned out to be wrong in a load-bearing way.

- **What was wrong**: the first pass's own fix-log text claimed "the frontend's client-side `recompute()` unconditionally zero-fills every rate block's full field set on every save, including untouched ones." Re-reading `ITR3BusinessAuxiliaryManager.tsx`'s `recompute()` function line-by-line found this false: it only ever writes a rate block's small set of DERIVED fields (`Total`/`FullRateDeprAmt`/`HalfRateDeprAmt`/`TotalDepreciation`/`NetAggregateDepreciation`/`WDVLastDay` for DPM 15/30/40; a similar smaller set for DOA), and only once that block already exists at all (i.e. the taxpayer typed into at least one of its own raw-input fields — an untouched block stays genuinely absent, not zero-filled, matching the original design intent). Every OTHER required leaf field (`AdditionsGrThan180Days`, `RealizationTotalPeriod`, `DepreciationAtFullRate`, `DepreciationAtHalfRate`, `DepDisAllowUs38_2`, `ProportionateAggDepreciation`, `ExpdrOnTrforSaleAsset`, `CapGainUs50`, and Land's own `WDVLastDay`) is left genuinely `undefined` in the frontend's own state whenever the taxpayer correctly leaves that specific input blank (no additions this year, no half-rate depreciation, no disposal) — it is never auto-filled with an explicit 0.
- **The consequence**: `_dpm_rate_detail()`'s original `return dict(detail)` passed whatever subset of fields the frontend actually sent straight into `ScheduleDPM.model_validate(...)`/`ScheduleDOA.model_validate(...)`. For this entirely ordinary, expected shape of real input — a block with SOME fields filled and others correctly left blank — Pydantic raised a hard `ValidationError` ("Field required") and the whole JSON-generation pipeline crashed for that taxpayer.
- **The fix**: `_dpm_rate_detail()` now returns a dict built from a fixed 21-key superset of every leaf field across all three `DepreciationDetail` shapes (the full DPM/DOA block, and the smaller Rate45/Land shapes), defaulting any absent key to `Decimal("0")` via `_decimal_value(detail.get(key))` — matching the official JSON schema's own declared per-field `"default": 0` for every one of these fields (confirmed directly against the schema's `required`/optional split, not assumed). Extra keys a smaller model (e.g. Rate45, which has no half-rate fields at all) doesn't declare are silently ignored by Pydantic (none of these classes set `extra="forbid"`), so one dict construction safely serves every shape. The Land block (only `WDVFirstDay`/`WDVLastDay`, no derived-field precedent at all on the frontend) got the identical treatment inline.
- **Verification**: `test_realistic_partial_block_with_untouched_fields_still_validates` (a block with only `WDVFirstDay` + the frontend's own derived fields, every other required field absent — confirmed genuinely failing pre-fix via `git stash`, a clean `ValidationError`, not just a wrong value) plus two new full-builder-and-schema-validation tests that hadn't existed before this check: `test_rate45_special_block_validates_against_official_schema` (the smaller 11-13-field shape, never previously exercised past the typed-model layer) and `test_all_doa_asset_categories_together_validate_against_official_schema` (Land + Building + FurnitureFittings + IntangibleAssets + Ships together). ITR-3 suite: 124 passed (up from 118). Full combined regression: same pre-existing ITR-2/26AS baseline failures only, zero new failures. Fix-log text above corrected in place (the "unconditionally zero-fills... including untouched ones" claim was the specific inaccuracy) rather than left standing alongside the correction.

---

### Schedule 19 — Schedule ESR (Expenditure on Scientific Research) — ✅ closed 2026-09-18

**What was found**: the flagged hypothesis from Schedule 15-18's closure was verified directly, not assumed, and confirmed exactly right. The mapper read `draft.itr3BusinessWorkspace.scheduleESR` (a typed field with **zero frontend writers anywhere**, confirmed by a repo-wide grep) `or draft.itr3BusinessWorkspace.core.get("ScheduleESR")` — but the real, working editor (`ITR3BusinessAuxiliaryManager.tsx`, whose own manifest already covers `ScheduleESR` end-to-end) writes into `draft.itr3BusinessWorkspace.auxiliary["ScheduleESR"]` instead. Traced the full write path to be certain, not just the component itself: `ITR3BusinessAuxiliaryManager`'s `data`/`onChange` props are bound in `ITR3BusinessWorkspace.tsx` to its own `auxiliary`/`onAuxiliaryChange` props, which `BusinessProfessionEntryManager.tsx` wires to a `BusinessProfessionScheduleData.ITR3Auxiliary` field, which `ITRComputationPage.tsx`'s `BusinessTab.handleChange` writes straight into `model.draft.itr3BusinessWorkspace.auxiliary` — the identical root cause already fixed for Schedule DPM/DOA (#15-18), now confirmed for ESR specifically rather than inferred by analogy alone.

A second, independent bug, the same shape as DPM/DOA's own "realistic partial input" crash: the frontend's `pathSet()` helper only ever creates the exact nested path a taxpayer's edit touches (confirmed by reading it directly) — a return using only some of Schedule ESR's 9 section rows (`Section35_1_i` through `Section35_CCD`) leaves the untouched section keys genuinely **absent** from the saved draft, not present with zero values. The official schema requires all 10 keys (9 sections + the frontend-computed `TotUs35` "Total" row) unconditionally, so validating the raw dict directly would have crashed JSON generation for this entirely ordinary case, exactly as it did for DPM/DOA before that fix.

A third gap, independent of the mapper dead-end: the typed `ESRDeductionDetail`'s three leaf fields (`AmtDebPL`/`AmtUs35Allowable`/`ExcessAmtOverDebPL`) carried no `ge`/`le` bound at all, despite the official schema placing `minimum: 0, maximum: 99999999999999, default: 0` on all three — the same bound-fidelity gap already found and fixed for Schedules 8/9/10.

**The tracker's own prior finding — "deduction not reflected in tax" — was investigated and found STALE, not true as of this push.** Schedule 14's own full closure (earlier the same day) had already wired Schedule BP's item 28 (`DebPLUs35ExcessAmt`, "deduction u/s 35/35CCC/35CCD in excess of the amount debited to P&L") into `compute_pgbp()`'s real `total_additions` computation — so the deduction DOES already reach tax, contrary to the stale note. But it does so via an **independently-entered** Schedule-BP-workspace field, not derived from Schedule ESR's own typed total at all — and the official form's own item 28 text explicitly cites this figure as literally **"item x(4) of Schedule ESR"** (the ESR schedule's own "Total" row, column 4), not an independently-re-derived same-concept number, the identical citation style already used for items 25/29/30's own Part-A-OI cross-references. Two independently-entered numbers for what the form itself says is one number is a real drift risk a taxpayer filling in both schedules could hit.

**What was fixed:**
- `app/engine/draft_to_itr3_input.py`: `esr_source`'s lookup chain gained `or draft.itr3BusinessWorkspace.auxiliary.get("ScheduleESR")`. New `_normalize_esr_source()` defaults every section/leaf a taxpayer left untouched to 0 (matching the schema's own per-field default) and **recomputes `TotUs35` server-side** as the sum of the 9 section rows, rather than trusting whatever the frontend's own client-side `recompute()` last sent — deliberately more defensive than DPM/DOA/DEP/DCG's own derived-value handling, since this specific total now also feeds real tax computation (below), not just disclosure. Returns `None` (schedule omitted entirely, not a fabricated zero-filled stub) when every leaf across all 9 sections is genuinely zero, matching the established DPM/DOA "an explicitly all-zero block must not be mistaken for a real one" precedent. `_business_income()` gained an optional `schedule_esr` parameter: when Schedule ESR data exists, `section35_excess_deduction` (feeding item 28) is now sourced from `schedule_esr.DeductionUs35.TotUs35.DeductUs35.ExcessAmtOverDebPL` instead of the raw workspace field — falling back to the original `regular.get("DebPLUs35ExcessAmt")` read when no ESR data exists at all, so a taxpayer who only ever used Schedule BP's own quick field keeps working exactly as before this push.
- `app/schemas/itr3.py`: `ESRDeductionDetail`'s three leaf fields gained `ge=0, le=99999999999999` (schema-matched bounds); `ESRDeductionDetail`/`ESRDeduction`/`ESRDeductionUs35`/`ScheduleESR` all gained `model_config = ConfigDict(extra="forbid")`, matching the Schedule 9 precedent of failing fast on a future field-name mismatch rather than silently misshaping the JSON.
- `app/engine/itd/itr3.py`: no functional change needed (`_schedule_esr()` was already correct, just permanently starved of real input) — but found and removed a genuinely harmless but confusing **duplicate definition** of `_serialize_schedule_model()` sitting a few lines below the first, silently shadowing it at module load (both were byte-for-byte identical, so this was never a live bug, unlike the earlier CG-schedule session's own dead-code find) — cleaned up as a zero-risk drive-by fix directly adjacent to this schedule's own edit.

**Design decision made explicitly**: deriving item 28 from Schedule ESR's own total, rather than leaving it as Schedule 14's originally-decided "entered directly" field, is a deliberate **revision** of that earlier decision — justified here because the official form's own text ties the two figures together by explicit citation (unlike items 19/23/29/30, which Schedule 14's closure correctly left as independently-entered since nothing on the form claims they equal some OTHER schedule's own total). The fallback to the raw field when no ESR data exists preserves every already-tested Schedule 14 scenario unchanged.

**Verification**: 12 new tests in `tests/test_itr3_schedule_esr.py` — the auxiliary-workspace dead-end fix; realistic partial input (2 of 9 sections touched, confirms the other 7 default to 0 and `TotUs35` is correctly recomputed as their sum); an explicitly-all-zero schedule correctly omitted; no-data-at-all stays `None`; `_normalize_esr_source` returns `None` for an empty mapping; `section35_excess_deduction` correctly derived from ESR's total (including proof that a stale/conflicting raw `DebPLUs35ExcessAmt` value is genuinely overridden, not merely coincidentally equal); the no-ESR-data fallback path (both through the full mapper and a direct unit-level call to `_business_income`); a full end-to-end test validating `ScheduleESR` against the real official schema AND confirming `ITR3ScheduleBP`'s own `DebPLUs35ExcessAmt` equals ESR's computed total in the same generated document; the omitted-when-no-data case at the full JSON level; bound-fidelity (`ge`/`le` rejection plus the schema's own default-0 construction); and `extra="forbid"` rejecting an unknown field. All genuinely failing pre-fix via `git stash` (stashing only the three source files, keeping the new test file) — a clean `ImportError` on the new `_normalize_esr_source` helper, matching the exact same accepted proof-of-failure category already used for Schedule DPM/DOA's own crash-bug tests. `tests/test_itr3_*.py tests/test_cyla.py tests/test_bfla.py` together: 150 passed, 0 failed (12 of these are the new ESR tests). No ITR-3 validator touched; no ITR-1/2/4 code touched.

**Update (2026-09-18, same day): three further bugs found on re-verification, prompted directly by the user asking "recheck if Schedule 19 is implemented end-to-end"** — re-deriving the claim from the real frontend/backend behavior rather than trusting the closure just written above, per this push's own meta-lesson.

- **Bug found**: the frontend's `esrFields()` marks `AmtUs35Allowable`/`ExcessAmtOverDebPL` as `readonly` (implying "auto-computed"), but nothing computes them anywhere — `recompute()` (read in full) only ever derives `TotUs35`, never a per-section Allowable/Excess. So these two leaves are genuinely absent for every real taxpayer interaction, and the first pass's "default an absent field to 0" policy (correct for every OTHER field in this codebase) was WRONG here specifically: it would have silently disclosed a Rs.0 allowable deduction against real, entered R&D expenditure. Verified against actual current law (WebSearch, cross-checked against secondary sources since the government's own incometaxindia.gov.in blocks automated fetches) that every one of Schedule ESR's 9 section-35 sub-clauses' historical 150%/200% weighted deductions were phased down to a flat 100% by Finance Act 2020, effective AY 2021-22, with no subsequent reversal — so the correct default for an absent `AmtUs35Allowable` is `AmtDebPL` itself (100%), not 0.
- **Bug found, genuinely official-AY-2026-27-sourced** (per the user's explicit instruction to use only official AY 2026-27 references): re-extracted `Reference Docs by CBDT & ITD/Official Validations/CBDT_e-filing_ITR-3_Validation Rules_V1.0_AY 26-27 (1).pdf` directly and found rule #354, quoted verbatim: *"If 'New Tax Regime' is selected, then in schedule ESR at column 3, amount cannot be more than zero for section 35(1)(ii), 35(1)(iia), 35(1)(iii), 35(2AA) and 35(CCC)."* The flat-100%-always assumption above is therefore only correct for the OLD regime — under the NEW regime, these 5 third-party-contribution sections (payments to research associations/universities/National Laboratory, agricultural-extension sponsorship) are disallowed entirely, the same principle as 80G/80C being unavailable under s.115BAC. The other 4 sections (the taxpayer's own direct R&D/skill-development expenditure — 35(1)(i)/(iv), 35(2AB), 35CCD) remain 100% allowable under either regime.
- **A regime-comparison bug in the FIX for the bug above, caught only because a dedicated new-regime test was written and actually exercised the path** (not caught by code review alone): the first attempt wrote `str(values["tax_regime"]) == "new"` — genuinely always `False`, since `values["tax_regime"]` is a live `TaxRegime` enum member post-`model_dump()` and Python's `Enum.__str__` returns `"TaxRegime.NEW"`, not `"new"` — silently defeating the entire regime check (every return would have been treated as old-regime). Fixed to the established codebase convention, a direct value comparison (`values["tax_regime"] == "new"`, str-Enum equality), matching how every other regime check in `draft_to_itr1_input.py`/`calculators/itr3.py` already does it.
- **A real tax-computation consequence of the regime-disallowance bug, found by tracing the fix through to its end rather than stopping at the disclosure layer**: when a new-regime section is disallowed, `AmtUs35Allowable` (0) falls below `AmtDebPL` — but that expenditure was ALREADY debited to (and reduced) the P&L account's own net profit figure. Official rule #286, also freshly re-extracted from the same AY 2026-27 validation-rules PDF: *"Schedule BP, sl no 24(e) should be minimum of Absolute value of total of negative values of 'col 3 - col 2' of all fields in Schedule ESR."* Without adding this shortfall back into taxable business income, a new-regime taxpayer's disallowed R&D expenditure would silently escape tax entirely — not a disclosure gap, a real understatement of `total_business_income`. Fixed via a new `_esr_shortfall_addback()` helper, folded additively into `other_additions` (Schedule BP item 24) — additive with, not replacing, the taxpayer's own separately-entered item 24 figure (item 24 is a broad "any other expense not allowable" bucket ESR's shortfall is only one possible contributor to, unlike item 28's exclusive ESR citation).

**Verification**: 4 new tests added to `tests/test_itr3_schedule_esr.py` (16 total) — `test_esr_new_regime_disallows_five_third_party_contribution_sections` (the 5 disallowed sections come out at exactly 0 allowable under new regime; the other 4 stay 100%), `test_esr_old_regime_allows_all_nine_sections_at_100_percent` (the new-regime restriction does not leak into old-regime returns), `test_esr_new_regime_shortfall_added_back_into_business_income` (the disallowed amount reaches `other_additions`, additively with a real separately-entered item-24 value), `test_esr_old_regime_no_shortfall_addback` (a confirmation test — passes even pre-fix, since old regime never produces a shortfall to add back; documented as such rather than miscounted as a bug-catching test). 3 of the 4 confirmed genuinely failing pre-fix via `git stash` (source file only); the 4th (`_normalize_esr_source`'s own empty-mapping test) failed with a clean signature-mismatch `TypeError`-shaped collection error from the new required `is_new_regime` parameter, the same accepted proof-of-failure category as before. `tests/test_itr3_*.py tests/test_cyla.py tests/test_bfla.py` together: 154 passed (up from 150), 0 failed. Full combined regression re-run after this update: see the commit's own verification note. No ITR-3 validator touched; no ITR-1/2/4 code touched.

---

### Schedule 20 — Schedule CG (`ScheduleCGFor23`) — ✅ closed 2026-09-18

**Note on prior work**: Schedule CG already had substantial real work behind it before this tracker-driven push began (git history shows a `Close ITR-2 Schedule CG gaps; continue ITR-3 canonical pipeline build-out` commit, and further per-category work: A3 equity/STT, A6/B9 generic other-assets, B6 NRI 112/115 securities, buyback-loss blocks, Table F accrual dates, PTI STCG/LTCG disclosure, all confirmed present and working by reading `_schedule_cg_for23_typed()` directly before assuming anything from the tracker's own stale prior-finding note). This entry covers only what THIS round closed, verified by re-deriving the current state directly rather than trusting the table's own inherited description.

**What was found**: the tracker's own prior-finding note ("dead fallback fn `_schedule_cg_for23` referenced but never defined") was verified directly and confirmed real — `build_itr3_json`'s branch `_schedule_cg_for23_typed(cg_data, typed_input) if typed_input is not None else _schedule_cg_for23(cg_data)` called a function that a repo-wide grep confirms is never defined anywhere, a genuine `NameError` waiting to happen for any caller omitting `typed_input` (confirmed live: `tests/validate_schemas.py`'s own `test_itr3()` — a manual schema-validation script, not pytest-collected in normal runs, hence never caught — constructs exactly this scenario, though it now fails even earlier for an unrelated, pre-existing `BFLossItem` construction bug in that same script, left untouched as out of scope).

Reading the rest of `_schedule_cg_for23_typed()` directly (not assuming from the tracker) found five further real gaps, all still hardcoded zero/empty despite real source data existing on the draft:
1. **A2/B2 (slump sale, section 50B)** — `SlumpSaleInStcg`/`SlumpSaleInLtcgDtls` hardcoded all-zero. Genuinely ITR-3-only (section 50B requires a business/undertaking; confirmed absent from ITR-2's own official form text, unlike every other CG category closed so far) — no ITR-2 precedent to port, needed a new schema/mapper/builder from scratch.
2. **A4 (NRI STCG shares/debentures, first proviso to s.48)** — `NRITransacSec48Dtl` hardcoded zero despite the frontend's own `stSection48` aggregate card (`nriSttPaid`/`nriSttNotPaid`) already capturing real data.
3. **B3-proviso (NRI LTCG without indexation)** — `NRIProvisoSec48` hardcoded zero despite `ltNriProviso48` (`ltcgWithoutBenefit`/`deduction54F`) already captured.
4. **B8 (NRI foreign exchange asset, s.115F)** — `NRISaleofForeignAsset` hardcoded zero despite `ltForeignAssets` rows (`saleValue`/`deduction115F`) already captured.
5. **`DeducClaimInfo.TotDeductClaim`** — hardcoded 0 despite the calculator's own `compute_exemptions()` already producing a real, non-fabricated exemption total for the identical DeducClaimDtls rows this same block already discloses.

Items 2-4 already had an exact, working, already-tested ITR-2 precedent to port directly rather than re-derive: `app/engine/draft_to_itr2_input.py::_map_cg_nri_proviso_48` (mapper) and `app/engine/itd/itr2.py::_nri_proviso_48`/`_nri_foreign_asset` (builder helpers) — both forms read the identical `ReturnDraft.capitalGainsSchedule.stSection48`/`ltNriProviso48`/`ltForeignAssets` fields and disclose them under the identical official schema shape (confirmed by direct schema introspection of both forms' JSON schema files, not assumed from the shared field names alone).

**What was fixed:**
- `app/schemas/itr3.py`: added `ITR3SlumpSaleRow` (fmv_11uae_2/3, net_worth, exemption_amount — the last LTCG-only, since the official form's own A2 formula has no exemption sub-item at all: `"2c = 2aiii - 2b"`, no "2d") and `cg_slump_sale_stcg`/`cg_slump_sale_ltcg` list fields on `ITR3Input`; added the six NRI bare-entry fields (`cg_nri_stcg_stt_paid`/`cg_nri_stcg_stt_not_paid`/`cg_nri_ltcg_without_indexation`/`cg_nri_ltcg_deduction_54f`/`cg_nri_115f_sale_value`/`cg_nri_115f_deduction`), matching ITR-2's own field names exactly for direct reuse.
- `app/engine/draft_to_itr3_input.py`: new `_map_slump_sale()` reads `capitalGainsSchedule.stSlumpSale`/`ltSlumpSale` raw rows into typed `ITR3SlumpSaleRow` lists; `_map_cg_nri_proviso_48` imported directly from `draft_to_itr2_input.py` and spread into the `ITR3Input(...)` construction (`**cg_nri_proviso_48`), identical to how `cg_buyback_losses` was already wired.
- `app/engine/itd/itr3.py`: new `_slump_sale_block()` aggregates however many slump-sale rows a taxpayer entered into the schema's single summary object (the official schema has no per-transaction array for this item) — `FullConsideration` = sum of the higher of Rule 11UAE(2)/(3) FMV per row, `Balance`/`SlumpBalance` = `FullConsideration - NetWorthOfDivision`, LTCG additionally subtracts the summed 54EC/54F exemption (the frontend row captures one flat amount with no section sub-code, so the schema-optional per-section breakdown array is omitted rather than guessed at — only `ExemptionGrandTotal`, the schema's one required field, is emitted). `_nri_proviso_48`/`_nri_foreign_asset` imported directly from `itd/itr2.py` (duck-typed against `ITR3Input`, which now carries the identical attribute names) and wired into `NRIProvisoSec48`/`NRISaleofForeignAsset`; `NRITransacSec48Dtl` now reads the two typed NRI-STT fields directly. `TotDeductClaim` now reads `cg_result.exemptions.total_exemption` (the calculator's own authoritative figure), matching ITR-2's own established `total_exempt` approach exactly. The dead `_schedule_cg_for23` branch removed entirely — `_schedule_cg_for23_typed` already raises a clear `ValueError` when `typed_input` is `None`, so calling it unconditionally removes the `NameError` risk with no behavior change for any currently-working path (confirmed: `ScheduleCGFor23` was already unconditionally emitted for every ITR-3 return regardless of `typed_input`'s presence, grouped under this builder's own pre-existing "Always-present conditional schedules" section — not something this fix altered).

**Design decisions made explicitly, not left implicit:**
- **`TotDeductClaim` is a real improvement over the prior hardcoded 0, but not yet a fully comprehensive sum across every CG exemption sub-type.** The calculator's `compute_exemptions()` call (both forms) only ever received 4 of the (now) 5+ exemption categories touching this schedule (54/54B/54EC/54F) — 115F, the new slump-sale 54EC/54F, and the other-assets-bucket 54D/54F/54G/54GA (added in an earlier session) are not yet folded into that same total. Flagged here rather than silently claimed complete; a genuine follow-up, not a regression (the figure is still strictly more accurate than the 0 it replaced).
- **A7/B10 (unutilized Capital Gains Accounts Scheme deposit, deemed STCG/LTCG) and A9/B12 (DTAA-rate claims) are left at their prior hardcoded values this round** — both are sizeable, independently-scoped pieces of work (A7/B10 needs a flag + prior-year-deposit row list; A9/B12 needs the full DTAA row shape — amount, item number, country, article, treaty rate, TRC, Act section/rate, applicable rate — ITR-2 already has a working `cg_stcg_dtaa_entries`/`cg_ltcg_dtaa_entries`/`_map_cg_dtaa_entries` precedent to port, matching this round's own NRI-proviso-48 approach) rather than deferred for any architectural reason. Left for this schedule's own next round rather than force everything into one sitting, consistent with how Schedule BP's own closure was itself split across two passes.
- **The current-year loss set-off matrix (`TotLossSetOff`, and `InStcgAppRate`/`InStcgDTAARate`'s own set-off sub-fields) remains deliberately disclosure-incomplete**, same reasoning already established and accepted at Schedule BP's own Part E closure: the calculator exposes loss magnitudes, not a per-target set-off breakdown, and populating this without that breakdown risks a worse, half-right disclosure than the current honest zero.

**Verification**: 9 new tests in `tests/test_itr3_schedule_cg.py` — three unit-level `_slump_sale_block()` tests (STCG has no exemption sub-item; LTCG's exemption reduces the final figure; multiple rows aggregate correctly into the one schema object), a mapper-level test (`_map_slump_sale` reads the raw draft rows into typed rows correctly), a full end-to-end schema-validation test for slump sale, a full end-to-end test for the three NRI blocks together (including summing `ltForeignAssets` across multiple rows), a zero-data confirmation test (the NRI blocks stay present-but-zero, not omitted, since they are required sub-objects of an always-present schedule), the `TotDeductClaim` fix (with an in-line note on why the test sets the transaction's legacy `deduction_us54` scalar rather than the richer `exemptions` claim list — the calculator's own `exempt_54` accumulator reads the legacy field, confirmed by reading `calculators/itr3.py` directly, not assumed), and the dead-fallback-function fix (`_schedule_cg_for23_typed(None, None)` now raises a named `ValueError`, not an unrelated `NameError`). All confirmed genuinely failing pre-fix via `git stash` (source files only) — a clean `ImportError` on the new `_map_slump_sale` function. `tests/test_itr3_*.py tests/test_cyla.py tests/test_bfla.py` together: 163 passed (up from 154). Full combined regression: 2378 passed, same 11 pre-existing failures + 1 pre-existing error as the established baseline, zero new failures. No ITR-3 validator touched; no ITR-1/2/4 code touched (ITR-2's mapper/builder logic was imported and reused, not modified).

**Update (2026-09-18, same day): closed A7/B10 (unutilized CGAS deposit) and A9/B12 (DTAA-rate claims), the two items explicitly left open above.**

**A9/B12** ported ITR-2's own already-shipped local `_cg_dtaa_rows()` helper and `chargeable_in_india`/DTAA-total logic verbatim into a module-level `_cg_dtaa_rows()` in `itd/itr3.py`, wired against the new `cg_stcg_dtaa_entries`/`cg_ltcg_dtaa_entries` fields (`ITR3Input`, reusing the shared `CGDtaaEntry` type) and the existing `_map_cg_dtaa_entries()` mapper (imported directly from `draft_to_itr2_input.py`, reading the identical `capitalGainsSchedule.stDtaa`/`ltDtaa` fields both forms already share). **Found and flagged, not fixed, a real pre-existing gap while writing this round's own schema-validation test**: `CGDtaaEntry.item_no_incl` has no enum constraint at the Pydantic level, but the official JSON schema restricts it to a fixed, per-form, per-ST/LT list of item-number labels (e.g. ITR-3's STCG table only accepts `A1e`/`A2c`/`A3ie`/.../`A8c`) — and the shared mapper's own default for a blank frontend field, `"NA"`, is not a member of that enum for either form, so a real taxpayer relying on that default produces schema-invalid JSON for this one field. Logged in the cross-form-issues section (the mapper lives in ITR-2's own file) rather than fixed as a side effect.

**A7/B10**: unlike every other item closed this schedule, the LTCG side (B10) had **no existing ITR-2 precedent at all** — confirmed by reading `itd/itr2.py` directly, ITR-2's own B10-equivalent is itself still hardcoded (`"UnutilizedLtcgFlag": "N"`, zeros), also logged as a cross-form issue. Built both sides fresh: `ITR3UnutilizedCGRow` (new typed row: prev-year-transferred, section-claimed, year-asset-acquired, amount-utilized, amount-unutilized) plus `cg_stcg_unutilized_flag`/`cg_ltcg_unutilized_flag`/`cg_stcg_unutilized_deposits`/`cg_ltcg_unutilized_deposits` on `ITR3Input`, mapped from the draft's own already-typed `stUnutilizedFlag`/`stUnutilized`/`ltUnutilizedFlag`/`ltUnutilized` fields (whose Y/N/X flag literal already matches the official schema's own enum exactly — a rare case where the draft schema was already schema-shaped with zero mapper effort). Direct schema introspection of both `UnutilizedCgPrvYrStcg`/`UnutilizedCgPrvYrLtcg` definitions found the two tables' own valid-section enums genuinely DIFFER (STCG: 54B/54G/54GA only; LTCG: 54/54B/54D/54F/54G/54GA/54GB) and that only the LTCG table requires `AmtUtilized` on every disclosed row (STCG leaves it optional) — both handled explicitly in the new shared `_unutilized_cg_block()` builder helper via a `valid_sections`/`require_amt_utilized` parameter pair, not silently assumed identical. `AmtDeemedStcg`/`AmtDeemedLtcg` sum every row's `amount_unutilized` regardless of whether that row's free-text year/section label happens to validate against the enum (a real deemed capital gain doesn't stop being real because a label doesn't parse), while the per-row disclosure array only includes the schema-valid rows.

**Verification**: 7 new tests appended to `tests/test_itr3_schedule_cg.py` (16 total) — DTAA rows reaching the JSON with the chargeable/not-chargeable split computed correctly, a full schema-validation pass (which is what caught the `item_no_incl` enum gap above — the test itself was written using a schema-valid label once the gap was found, with an in-line note explaining why), the no-data-omitted case; a valid unutilized-STCG row reaching the JSON and validating, an invalid-section row still counting toward the deemed total but correctly dropped from disclosure, the LTCG table's own wider section set and `AmtUtilized` requirement, and the no-data default (flag "N", zero, no fabricated `UnutilizedCg` block). All 5 fix-dependent tests confirmed genuinely failing pre-fix via `git stash` (source files only) — `ImportError`s on the new `ITR3UnutilizedCGRow` type and a `KeyError`/wrong-value on the DTAA fields; the two no-data confirmation tests correctly pass in both states, documented as such. `tests/test_itr3_*.py tests/test_cyla.py tests/test_bfla.py` together: 170 passed (up from 163). Full combined regression: 2385 passed, same 11 pre-existing failures + 1 pre-existing error as the established baseline, zero new failures. No ITR-3 validator touched; no ITR-1/2/4 code touched (both cross-form gaps found this round are logged, not fixed).

**Update (2026-09-18, same day): closed B4b (112A's own s.54F deduction) and `SaleofLandBuild.TotalExcessTax`/B1eii disclosure wiring, then found and fixed a severe, previously-undiscovered tax-computation defect while verifying the latter — section 112 (LTCG other than 112A) was never taxed at its own special rate anywhere in ITR-3.**

**B4b and TotalExcessTax (disclosure fixes)**: `equity_112a` block's `DeductionUs54F`/`CapgainonAssets` now compute a real s.54F deduction against 112A-classified `CGTransaction` rows (summed via the shared `_exemption_claim_total()` helper, attributable only to rows the calculator itself classified into the 112A basket -- `cg_112a_scrips`' own explicit path has no `exemptions` field at all, a separate, narrower, undisturbed limitation), ported verbatim from ITR-2's own already-working `itd/itr2.py` logic. `SaleofLandBuild.TotalExcessTax` now reads `ltcg.total_excess_tax_112_1a` (already computed per-asset by `compute_ltcg()`'s own section 112(1)(a) second-proviso comparison, previously just never read by this builder) instead of a hardcoded 0 -- matching ITR-2's own one-line wiring exactly.

**The severe defect, found while confirming the TotalExcessTax fix would ever produce a nonzero value**: `total_excess_tax_112_1a` was ALWAYS zero in practice, because `calculators/itr3.py` never passed `is_resident` to `compute_ltcg()` at all (always defaulted `False`). Investigating why led to discovering something much larger: **ITR-3's calculator had NO working section-112 (LTCG-other-than-112A, 12.5%) special-rate tax computation at all.** `special_rates.py::compute_112()` exists, is correct, and is called by ITR-2 -- but was never called anywhere in `calculators/itr3.py`. A direct calculator call confirmed the severity: an Rs 80,00,000 LTCG-other transaction (old regime) produced `special_rate_tax=0`, `slab_tax=Rs 22,12,500` -- the entire gain was taxed at ordinary slab rates instead of section 112's 12.5% (Rs 10,00,000), a large overstatement of tax on that income. **The user explicitly authorized checking and fixing BOTH ITR-2 and ITR-3 for this specific issue** ("Fix it properly now, it's a correctness bug check both the ITR-2 and the ITR-3 as they share same schedule, refer both of their ITR FORM and schema individually for schedule CG") -- a deliberate, one-time exception to this push's standing "ITR-3 only, never touch ITR-1/2/4" rule.

**Root causes, all in `calculators/itr3.py`, none in the official form/schema themselves** (both forms' own Schedule SI table and Part D tax-computation sequence were re-confirmed directly against their official JSON schemas -- section 112's 12.5% rate, SecCode "22"/"21", and the section 112(1)(a) second-proviso mechanism are identical between both forms; ITR-3's own defect was purely in the calculator never invoking this already-correct, already-shared machinery):
1. `compute_ltcg()` never received `is_resident`, so the second-proviso relief could never trigger.
2. A second, independent defect blocked even a correctly-passed `is_resident` from ever mattering: the land/building `CGAsset` construction in the transaction loop never populated `date_of_acquisition`/`date_of_transfer`/`stamp_duty_value`/`year_of_improvement` at all -- `compute_ltcg()`'s own `_parse_date("")` always returns `None`, so `eib_applicable` was unconditionally `False` regardless of residency. Also silently disabled the section 50C deemed-consideration override for ITR-3 land/building STCG/LTCG. Fixed alongside, mirroring ITR-2's own already-correct construction exactly.
3. CYLA/BFLA lumped EVERY capital-gain rate bucket (111A @20%, ordinary LTCG @12.5% under section 112, 112A, DTAA-rate CG) into a single generic `stcg_app_income`/`stcg20_income` bucket with zero rate differentiation -- confirmed by direct inspection, not assumed.
4. Schedule SI's `si_112a`/`si_111a` entries were built from RAW pre-CYLA/BFLA values (`cg_result.ltcg.taxable_112a`, `stcg_result.income_111a`), not post-loss-setoff values -- meaning any loss set off via CYLA/BFLA against the lumped bucket never actually reduced the special-rate tax (still computed on the full, un-reduced figure) while `normal_income`'s own `max(z, ...)` clip silently "wasted" the loss's benefit instead.
5. `cg_stcg_dtaa_entries`/`cg_ltcg_dtaa_entries` (wired for A9/B12 disclosure earlier this same day) and the NRI proviso-48/115F bare-entry fields (`cg_nri_stcg_stt_paid`/`cg_nri_ltcg_without_indexation`/etc., wired for A4/B3-proviso/B8 disclosure in an earlier round) were read by the ITD builder for disclosure only -- never merged into `stcg_result`/`ltcg_result` by the calculator, so both had zero effect on the actual tax computed despite being correctly shown on the filed JSON.

**What was fixed:**
- `app/engine/schedules/capital_gains.py`: extracted ITR-2's own private `_post_loss_cg_baskets()`/`_consume()` (calculators/itr2.py) into a new, public, form-agnostic `post_loss_cg_baskets()` (dropping its one genuinely-unused parameter, `non_cg_income_available_for_hp` -- confirmed unreferenced anywhere in the function body). ITR-2's calculator now imports and calls this shared version instead of its own private copy; ITR-2's own dedicated regression subset (`-k "itr2 or cyla or bfla or capital_gain"`, 783 tests) passed identically before and after the extraction, confirming byte-for-byte behavioral parity.
- `app/engine/calculators/itr3.py`: added `is_resident_or_nor` (same `residential_status != NON_RESIDENT` gate as ITR-2, correctly treating NOR as resident per section 6) and passed it into `compute_ltcg()`; fixed the land/building `CGAsset` construction (item 2 above); added the NRI-proviso-48/115F and DTAA merge block immediately after `stcg_result`/`ltcg_result` construction, ported verbatim from ITR-2's own calculator (mutating `income_111a`/`income_30per`/`income_125per_other`/`income_dtaa`/`total_stcg`/`total_ltcg` in place -- the DTAA entries re-tag part of an already-declared ordinary CG transaction for special-rate treatment, exactly like ITR-2's own scoping decision, not a separate additive income source); rebuilt the CYLA/BFLA sub-basket construction to correctly route `income_111a` to `stcg20`, `income_30per + income_app_rate` to `stcg_app` (ITR-3 has no FII/FPI assessee concept at all, so there is no genuine flat-30% STCG basket, unlike ITR-2), `income_125per_other + income_112a` to `ltcg125`, and each `income_dtaa` to its own DTAA sub-basket; BFLA now consumes each CYLA sub-basket's own post-CYLA residual directly (`cyla.stcg20_remaining` etc.) instead of a single re-derived lumped figure; called the new shared `post_loss_cg_baskets()` after BFLA and applied its `exemption_used` correction to GTI (mirroring ITR-2's identical two-step GTI correction); Chapter VI-A's `cg_112a_income`/`cg_111a_income` inputs now use the post-loss basket values, not raw pre-loss figures; rebuilt the Schedule SI section entirely from post-loss values -- `si_112a_entry` now uses `compute_112a_taxable()` (correctly disclosing the true pre-threshold gross alongside the post-threshold taxable amount, fixing a second, smaller pre-existing disclosure bug: the old `compute_112a(taxable, pre_exempted=True)` call silently disclosed the post-threshold amount as if it were the gross figure) instead of the bare `compute_112a()`; added the missing `compute_112()` call for the "112" bucket with the section 112(1)(a) second-proviso relief applied (`relief = min(total_excess_tax_112_1a, si_112_entry.tax_amount); si_112_entry.tax_amount -= relief`, capped exactly like ITR-2's own identical pattern); added DTAA STCG/LTCG Schedule SI rows via `compute_dtaa_stcg`/`compute_dtaa_ltcg`, ratio-allocated across each declared DTAA entry's own treaty rate exactly like ITR-2's identical allocation.

**Design decisions made explicitly:**
- The DTAA merge intentionally treats `cg_stcg_dtaa_entries`/`cg_ltcg_dtaa_entries` as a RE-TAG of an already-declared ordinary `cg_transactions` gain, not new additive income -- this is ITR-2's own documented scoping decision (Schedule CG's A8/B11 items are literally defined as "already counted in the underlying item, just re-tagged for rate purposes"), and this fix's own tests were initially written incorrectly (assuming additive income) until this was re-confirmed against ITR-2's calculator directly -- corrected before landing, not left as a latent test bug.
- The pre-existing gap where ITR-3's land/building `CGAsset.exemptions` (the per-asset rich claim list) is never populated from `CGTransaction.exemptions` -- unlike ITR-2's `_classify()`, which does populate it -- was found while reasoning through the second-proviso's own post-exemption ("1e"/"1ea") comparison, but is a narrower, disclosure-precision-only gap (the AGGREGATE exemption total is still correctly applied via the pre-existing `exempt_54`/`compute_exemptions()` legacy-scalar path) and was deliberately left untouched -- out of scope for a tax-rate correctness fix.
- The ITR-3-only non-speculative business-loss CYLA sign bug found during this investigation (item 4 in "Cross-form issues found, not fixed" above) is a distinct, Schedule-BP/CYLA cross-head wiring defect, not a Schedule-CG rate defect -- deliberately left unfixed and flagged rather than folded into this round.

**Verification**: new `tests/test_itr3_cg_tax_rate_fix.py` (8 tests) -- the core bug reproduced and fixed (Rs 80L LTCG-other now correctly produces Rs 10L special-rate tax and Rs 0 slab tax, not the reverse); a current-year HP loss correctly reducing the post-loss section-112 bucket's own special-rate tax (proving the loss is no longer "wasted"); the section 112(1)(a) second-proviso relief now applying for a resident and correctly NOT applying for an otherwise-identical non-resident transaction; NRI proviso-48 STCG entries now actually taxed at their correct 111A/slab rates; a chargeable DTAA LTCG entry now generating its own Schedule SI row at its own treaty rate (re-tagged out of the ordinary section-112 row entirely); a NIL-treaty-rate ("not chargeable in India") DTAA entry correctly vanishing from taxable income entirely; and a schema-validation test confirming both `ScheduleCGFor23` and `ScheduleSI` still validate against the official JSON schema with the new section-112/DTAA rows present. All 7 fix-dependent tests confirmed genuinely failing pre-fix via `git stash` (the three modified source files only, including the newly-shared `capital_gains.py`) -- concrete wrong-value/`KeyError` failures, not import errors. `tests/test_itr3_*.py tests/test_cyla.py tests/test_bfla.py` together: 177 passed (up from 170; +7 for this fix). Full combined regression: 2393 passed, same 11 pre-existing failures + 1 pre-existing error as the established baseline, zero new failures anywhere including ITR-2's own suite. No ITR-3 validator touched.

**Update (2026-09-19): fixed all three defects found by the comprehensive cross-form Schedule CG test suite (items 5-7 in "Cross-form issues found, not fixed" above), at the user's explicit request** ("okay then fix all of these" -- an explicit, one-time authorization to touch ITR-2's own code for item 6, mirroring the earlier CG-rate-bug exception). The user separately asked whether any of the three also existed on ITR-2; verified directly and confirmed only item 6 is ITR-2's own bug (items 5 and 7 are ITR-3-only -- ITR-2 already handles both correctly, confirmed by its own already-passing tests), so no additional ITR-2-side change was needed beyond fixing the shared function once.

**Item 5 (ITR-3 BFLA `str(enum)` bug)**: `calculators/itr3.py`'s `bf_list` construction changed from `"head": str(item.head)` to `"head": item.head.value if hasattr(item.head, "value") else str(item.head)`, matching ITR-2's own already-correct line exactly.

**Item 6 (shared `post_loss_cg_baskets()` CYLA-vs-BFLA staleness)**: `app/engine/schedules/capital_gains.py`'s `post_loss_cg_baskets()` changed `normal_stcg`/`section_111a`/the `"stcg_dtaa"` return key from reading `cyla.stcg30_remaining`/`cyla.stcg_app_remaining`/`cyla.stcg20_remaining`/`cyla.stcg_dtaa_remaining` (pre-BFLA) to reading the equivalent `bfla.*_remaining` fields (post-BFLA), matching what the LTCG-side buckets (`"112"`/`"ltcg_dtaa"`) already correctly did. This is ITR-2's own pre-existing bug (present since the function's introduction, confirmed via `git log -S` finding both the buggy and correct lines introduced in the same original commit with no later corrective commit) -- fixing it changes ITR-2's own live tax computation whenever a brought-forward STCG-head loss is absorbed into an STCG-rate bucket (111A/normal-rate/DTAA), which previously reduced GTI correctly but left that bucket's own Schedule SI special-rate tax untouched (an overtaxation bug). Given the sensitivity of touching ITR-2's own live-audited code, this was verified against ITR-2's own dedicated regression subset (`-k "itr2 or cyla or bfla or capital_gain"`) in addition to the full combined suite before being considered safe.

**Item 7 (ITR-3 STCG-land 54B exemption)**: two-part fix in `calculators/itr3.py`'s transaction-classification loop, both needed since they address two independent, compounding gaps: (1) the short-term land/building branch now accumulates `exempt_54b += tx.deduction_us54b` (STCG land/building may claim ONLY 54B per Schedule CG item A1d, unlike the long-term branch's four sections) -- without this, the claim never enters the aggregate `exemptions.total_exemption` pool at all; (2) the `CGAsset` construction for BOTH short- and long-term land/building rows now passes `exemptions=_normalized_land_exemptions(tx)` (the existing shared helper already used by ITR-2's own `_classify()`, imported fresh into `calculators/itr3.py`) -- without this, `post_loss_cg_baskets()`'s own `stcg_land_54b = sum(asset.exemption_total for asset in stcg.land_building)` STCG-bucket-TARGETING logic has nothing to read (it depends on the per-asset `.exemptions` list, never the scalar accumulators), so the exemption would have leaked entirely into the LTCG bucket instead of reducing STCG first as the statute requires. This same change closes the previously-separately-flagged, narrower "ITR-3 land/building `CGAsset.exemptions` never populated" disclosure-precision gap (Schedule 20's own "design decisions" list above) as a side effect, since it was the identical root cause.

**Verification**: `tests/test_schedule_cg_comprehensive.py` updated in place -- the dedicated bug-pinning tests from the 2026-09-18 entry were replaced with straightforward parametrized correctness tests (now passing identically on both `itr2`/`itr3`, since all three fixes bring ITR-3 to parity with ITR-2's own already-correct behavior, and item 6's fix improves ITR-2 itself too), plus two new tests (`test_brought_forward_stcg_loss_reduces_111a_bucket_including_schedule_si`, `test_brought_forward_stcg_loss_absorbs_stcg_before_ltcg`) proving item 6's regression fix explicitly. All 8 fix-dependent tests (`test_brought_forward_ltcg_loss_reduces_current_year_section_112[itr3]`, `test_brought_forward_stcg_loss_reduces_111a_bucket_including_schedule_si[itr2]`/`[itr3]`, `test_brought_forward_stcg_loss_absorbs_stcg_before_ltcg[itr2]`/`[itr3]`, `test_brought_forward_expired_loss_after_8_years_never_applies[itr3]`, `test_54b_on_stcg_land_reduces_stcg_not_ltcg[itr3]`, `test_leftover_54_exemption_after_stcg_reduces_ltcg[itr3]`) confirmed genuinely failing pre-fix via `git stash` (the two modified source files only). The full comprehensive suite: 83 passed (up from 79 -- 4 net new cases from re-parametrizing three previously form-restricted tests plus 2 new tests, minus discontinuing the now-obsolete bug-pin tests). `tests/test_itr3_*.py tests/test_cyla.py tests/test_bfla.py tests/test_capital_gains_loss_foundation.py tests/test_standalone_cg_schedule.py tests/test_schedule_cg_comprehensive.py` together: 284 passed. ITR-2's own dedicated regression subset (`-k "itr2 or cyla or bfla or capital_gain"`, given item 6 touches ITR-2's own live tax computation): 824 passed, same 11 pre-existing failures, zero new. Full combined regression: 2476 passed, same 11 pre-existing failures + 1 pre-existing error as the established baseline, zero new failures anywhere. No ITR-3 validator touched.
